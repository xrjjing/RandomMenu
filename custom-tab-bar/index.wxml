<t-tab-bar value="{{ value }}" theme="tag" split="{{ false }}" bind:change="handleChange">
  <t-tab-bar-item wx:for="{{ list }}" wx:key="value" icon="{{ item.icon }}" value="{{ item.value }}">
    {{ item.label }}
  </t-tab-bar-item>
</t-tab-bar>
