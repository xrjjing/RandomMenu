# 「今天吃什么」家庭点菜小程序 · 开发文档

> 项目代号：RandomMenu
> 使用者：本人及家人（约 2-6 人，非公开产品）
> 核心诉求：解决"每天做菜选择困难"，管理家庭菜谱，按原料找菜，随机转盘选菜，统计做菜记录

---

## 1. 项目概述

### 1.1 背景

家庭每天都要面对"今天吃什么"的问题。本小程序提供：

- **菜谱管理**：内置一批家常菜，家庭成员可自行上传（图片 / 原料 / 做法）
- **原料管理**：独立原料库，与菜谱多对多关联
- **按料找菜**：根据手头原料筛选可做的菜
- **随机选菜**：手动挑选或转盘随机抽取
- **做菜日志**：记录最终做的菜，按日 / 月 / 年统计，柱状图 / 饼图展示

### 1.2 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端框架 | 微信小程序原生 | 保留模板的 Less + resolveAlias 结构 |
| UI 组件库 | TDesign miniprogram | 模板已内置，圆角现代风格契合需求 |
| 后端 | 微信云开发（个人版，环境 `cloudbase-d2g5pab4nda21e944`） | 云文档库 + 云存储，**零云函数**（见 §5.1；MySQL 方案已评估后否决） |
| 图表 | echarts-for-weixin（按需构建） | 仅保留 bar / pie，放独立分包 |

### 1.3 设计原则

- **干净整洁**：浅灰背景（`#f5f6f7`）+ 白色圆角卡片（圆角 `24rpx`），大留白，低饱和
- **圆滑现代**：全局圆角、柔和阴影、主色建议暖橙（`#ff8c42` 一类"食欲色"），通过 TDesign CSS 变量统一
- **低门槛**：家人（含长辈）都能上手：大字号、少层级、关键操作有确认

---

## 2. 功能需求

### 2.1 模块总览

```
┌──────────────┬──────────────┬──────────────┬──────────────┬──────────────┐
│   菜谱模块    │   原料模块    │   选菜模块    │   统计模块    │   家庭模块    │
│ 菜单库 CRUD   │ 原料库 CRUD   │ 按原料匹配    │ 做菜日志     │ 昵称花名册    │
│ 高级/模糊搜索 │ 模糊查询      │ 转盘随机      │ 日/月/年图表 │ 家庭与别名    │
│ 菜品详情页    │ 勾选+即时新增 │ 手动选择      │ 柱状/饼状    │ 归属管理      │
│ 内置数据导入  │ 使用次数统计  │ 选择落账      │ 原料同步计数 │ 记录按家隔离  │
└──────────────┴──────────────┴──────────────┴──────────────┴──────────────┘
```

### 2.2 菜谱模块

**字段规则**

| 字段 | 必填 | 说明 |
|---|---|---|
| 菜名 | ✅ | 唯一性软校验：保存时查重，重名弹确认"已存在同名菜，仍要保存吗？" |
| 分类 | ✅ | `餐食` / `饮品` 二选一 |
| 图片 | ❌ | 0-5 张，单张 ≤ 1M，jpg / png / webp / gif |
| 原料 | ❌ | 从原料库勾选；可即时新增（见 §2.3） |
| 做法 | ❌ | 分步骤文本（步骤数组，每步一段，可增删排序） |

**功能清单**

- 列表页：分类 Tab（全部 / 餐食 / 饮品）+ 搜索框 + 卡片流（图、名、原料标签）
- 搜索：**菜名 / 原料名 / 做法文本** 三字段模糊匹配（正则 `i` 忽略大小写）
- 详情页：轮播图、原料标签、步骤列表、编辑 / 删除入口
- 删除：二次确认；**删除菜品不级联删除原料**（关联自然解除）；历史做菜记录保留（靠冗余字段，见 §4.4）
- 编辑：全字段可改；原料改动时同步维护两边关联

### 2.3 原料模块

- 每种原料有独立 `_id`，名称唯一（建议建立唯一索引，输入时 trim + 全半角归一后查重）
- 原料库管理页：列表（按使用次数 / 拼音排序）+ 新增 + 重命名 + 删除
- 删除原料：**不删除菜品**，仅解除菜品中的该原料引用（提示影响 N 道菜）
- 模糊查询：名称正则匹配，输入即搜
- **菜品编辑页内的原料选择器**（核心交互）：
  1. 输入框实时模糊搜索原料库
  2. 候选列表 checkbox 勾选 / 取消
  3. 无匹配结果时显示条目「➕ 新增原料"西红柿"」
  4. 保存菜品时统一执行 ensure：库内无此原料则先 `add` 再建立关联

### 2.4 选菜模块（首页）

**流程**

```
选原料(多选,可跳过) ──→ 匹配菜品列表 ──→ 手动选一道 ─┐
        │                                            ├─→ 确认落账(写 records)
        └──────────────────→ 转盘随机 ──→ 结果弹窗 ──┘
                              (可"换一个"重转)
```

**匹配规则（默认策略，可讨论）**

- **默认：交集匹配** —— 菜品原料与所选原料有交集即入选，按"匹配度"降序（匹配数 / 菜品原料总数）
- **可选：完全匹配** —— 菜品全部原料都在所选范围内（"就用这些料做"）
- 另按分类（餐食 / 饮品）过滤

**转盘规则**

- 候选 = 当前筛选结果；超过 12 道时随机抽 12 道上盘（盘面可读性优先）
- Canvas 绘制扇区 + 菜名，CSS/JS 控制旋转（5 圈 + 随机角度，ease-out 约 4s）
- 停止后弹结果：`就吃它了`（写记录）/ `换一个`（重转）
- 候选为空时转盘置灰并提示

**落账（写记录）**

- 手动选 / 转盘确认后写入 `records` 一条：菜品 +1、菜品所用原料各 +1（原料计数由统计聚合得出，不做物理累加字段，避免数据不一致）
- **当天没做就不写**，无空记录、无补录界面（保持简单；统计天然跳过无记录日期）
- **F25 提前定菜**：落账确认时可三选日期（今天/明天/后天，默认今天），明天/后天的菜写入对应日期、不计入统计；到当天自动变为“今日已定”（查询按日期动态分组，无需迁移）
- 首页展示“今明已定”卡片（F25 升级自“今日已定”）：今天/明天/后天三组，存在才显示该组；每行点「原料」弹出该菜原料列表（落账快照），点菜名直接跳详情页；撤销仅对“今天”组开放（撤销今天最后一条）

### 2.5 统计模块

**数据口径**：全部从 `records` 聚合，原始事实只有一份，统计永远可重算。

| 粒度 | 柱状图 | 饼图 |
|---|---|---|
| 日（默认今天） | 当日各菜次数分布 | 同左（按菜） |
| 月（默认当月） | 当月每日做菜次数趋势 | 当月菜品 Top N + 其他 |
| 年（默认当年） | 当年每月做菜次数趋势 | 当年菜品 Top N + 其他 |

- 切换器：`日 / 月 / 年` 粒度切换 + `柱 / 饼` 图型切换 + 日期前后翻页
- 额外页签：**原料榜**（所选时间范围内原料使用次数排行），对应"原料 +1"
- 图表数据由小程序端 aggregate 聚合（`group by dishName / ingredientNames / date 前缀`），无需云函数

### 2.6 家庭模块（多租户隔离，F24）

**数据归属**：菜谱/原料/图片全家庭共用；做菜记录（今日已定、撤销、统计）按家庭隔离。

- 身份：微信 openid（云函数 `login` 返回），首次打开自动入花名册（填昵称）
- 花名册 `members`：昵称 + 家庭归属 + isAdmin；未分配成员的数据进「未分配池」，功能照常可用
- 家庭 `families`：可新建/改名（别名任意、数量不限），仅管理员（isAdmin）可管理
- 记录隔离：`records` 落账时快照 `familyId`，今日卡/撤销/统计均按当前成员家庭过滤
- 管理页 `packages/family`：仅管理员可访问（页面守卫 + 数据库安全规则双保险）
- 存量历史记录：由管理员在管理页一次性「归池」后再划归家庭

---

## 3. 数据库设计（云开发 · 文档型）

### 3.1 集合 ER

```
┌───────────┐   多对多(双向冗余)   ┌──────────────┐
│  dishes   │ ──────────────────→ │  ingredients │
│ 菜品       │   dishes.ingredientIds / ingredientNames    │ 原料          │
└─────┬─────┘                     └──────────────┘
      │ 1..n（冗余 dishName 等）
┌─────▼─────┐   familyId（落账时快照）   ┌────────────┐
│  records  │ ────────────────────────→ │  families  │
│ 做菜日志   │                           │ 家庭（别名） │
└───────────┘                           └─────▲──────┘
┌───────────┐                                 │
│ members   │ ────────────────────────────────┘
│ 花名册     │  familyId 当前归属（'' = 未分配池）
└───────────┘
┌───────────┐
│ app_meta  │  系统元数据（内置数据导入锁）
└───────────┘
```

### 3.2 `dishes` 菜品集合

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 自动 |
| `name` | string | 菜名 |
| `category` | string | `meal` 餐食 / `drink` 饮品 |
| `images` | string[] | 云存储 fileID，≤3 |
| `ingredientIds` | string[] | 关联原料 ID |
| `ingredientNames` | string[] | 原料名冗余（展示 + 搜索用，见 §4.4） |
| `steps` | string[] | 做法步骤 |
| `isBuiltin` | boolean | 是否内置 |
| `_openid` | string | 创建者（自动写入，仅记录） |
| `createdAt` / `updatedAt` | Date | |

### 3.3 `ingredients` 原料集合

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 自动 |
| `name` | string | 归一化后唯一（建议建唯一索引） |
| `pinyin` | string | 可选，排序用（首版可省） |
| `createdAt` | Date | |

> 使用次数不落库，聚合 `records.ingredientNames` 统计。

### 3.4 `records` 做菜记录集合

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 自动 |
| `date` | string | `YYYY-MM-DD`（聚合分组键）；F25 起支持提前定菜：落账可传今天/明天/后天，非法值静默回退今天（`addCookRecord` 第三参） |
| `dishId` | string | 可为空引用（菜品删了记录仍有效） |
| `dishName` | string | 冗余，保证历史可读 |
| `ingredientNames` | string[] | 冗余快照，原料 +1 统计来源 |
| `familyId` | string | 落账时快照的家庭 `_id`（F24）；`''` = 未分配池；存量旧记录无此字段，由管理页「归池」补齐 |
| `createdAt` | Date | |

### 3.5 `app_meta` 元数据集合

| 字段 | 说明 |
|---|---|
| `_id: "seed_lock"` | 内置数据导入分布式锁，见 §5.3 |

### 3.6 `families` 家庭集合（F24）

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 自动 |
| `name` | string | 家庭别名（唯一，管理页校验重名） |
| `createdAt` | Date | |

- 数量不限、别名任意；仅管理员可新建/改名（安全规则兜底，见 §3.8）
- 菜谱/原料/图片不挂家庭（全家庭共用）；只有 `records.familyId` 与 `members.familyId` 引用家庭

### 3.7 `members` 花名册集合（F24）

| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 自动 |
| `_openid` | string | 云开发 add 时自动写入，天然唯一（一人一条） |
| `nickname` | string | 昵称（首次打开小程序时填写） |
| `familyId` | string | 当前归属家庭 `_id`；`''` = 未分配池 |
| `isAdmin` | boolean | 管理员标记（初始由使用者在云开发控制台手工设置，仅一次） |
| `createdAt` | Date | |

- 成员首次打开小程序自动注册：`ensureIdentity` 查无记录 → 首页引导填昵称 `registerMember`
- `member.familyId` 是查询口径：今日卡/撤销/统计都按它过滤；管理员调整归属只影响之后的落账与查询，不回溯历史记录

### 3.8 权限

原有四个集合（dishes / ingredients / records / app_meta）维持**自定义安全规则 `{"read": true, "write": true}`**（所有用户可读写）。这也是零云函数直连的前提——否则跨用户写库会被默认权限拦截。records 的家庭隔离由前端按 `member.familyId` 过滤，家庭内部互信。

管理类集合（F24）收紧为「仅管理员可写」，规则中数组换成管理员真实 openid：

```json
// families：读放开，写仅管理员
{ "read": true, "write": "auth.openid in ['你的openid']" }

// members：读放开，任何人可注册自己，改动（归属/管理员标记/删除）仅管理员
{ "read": true, "create": true, "update": "auth.openid in ['你的openid']", "delete": "auth.openid in ['你的openid']" }
```

> 依据官方安全规则文档：`in` 运算符支持 `auth.openid in ['zzz','aaa']` 常量数组形式，操作类型支持 read/write/create/update/delete 细分。配置路径：云开发控制台 → 数据库 → 对应集合 → 权限设置 → 自定义安全规则。配置后务必用非管理员手机验证：进管理页应提示仅管理员可访问。
> 退路：若个别环境对表达式规则的 `doc().update` 校验过严导致管理员操作被拒，可临时把 write 放回 `true`（前端页面守卫仍在），再排查规则。

---

## 4. 关键技术方案

### 4.1 请求层改造

`api/request.js`（wx.request 封装）不再需要，改造为 `api/db.js`：

```js
// 统一出口：集合引用 + 常用查询助手
const db = wx.cloud.database()
const dishes = db.collection('dishes')
// fuzzy(name) → db.RegExp({ regexp: escapeRegExp(kw), options: 'i' })
```

所有页面只 import 这一层的封装，不散落 SDK 调用。

### 4.2 模糊搜索

```js
// 菜谱搜索：三字段 OR
dishes.where(_.or([
  { name: fuzzy(kw) },
  { ingredientNames: fuzzy(kw) },   // 数组字段正则即元素匹配
  { steps: fuzzy(kw) },
])).get()
```

家庭数据量（菜品数百级）下正则全扫无性能问题，不建全文索引。

### 4.3 图片上传（≤1M，多格式）

```
wx.chooseMedia(count≤3)
  → 逐张校验 size ≤ 1MB
  → 超限则 wx.compressImage 循环降质(80→60→40) 直至达标
  → gif 不可压缩，超限直接拒绝并提示
  → wx.cloud.uploadFile(cloudPath: dishes/{日期}/{随机名}.{ext})
  → 存 fileID；<image> 可直接渲染 fileID
```

- 删除菜品时调用 `wx.cloud.deleteFile` 清理其云存储文件
- 上传失败：toast + 保留表单，可重试

### 4.4 关联与冗余策略（重要取舍）

小程序端 SDK **不支持联表查询（lookup 仅云函数可用）**。为保持零云函数架构：

- `dishes` 冗余存 `ingredientNames`（展示、搜索、落账快照都靠它）
- 写路径统一走 `api/db.js` 的 `saveDish()`：原料 ensure → 建关联 → 同步冗余字段，一处维护
- 原料重命名时批量更新引用它的 dishes（事务或逐条，家庭量级无压力）

### 4.5 内置数据（Seed）

- `data/builtin-dishes.js`：内置 JSON（建议 30-40 道家常菜 + 8-10 款饮品，覆盖 40+ 常见原料；首版不带图片，后续可补）
- **首次导入协议**（多设备防重复）：
  1. `app_meta` 中 `add({ _id: 'seed_lock', ... })` —— `_id` 冲突即说明别人已导入，跳过
  2. 抢到锁的设备：解析内置 JSON → 原料去重批量插入 `ingredients` → 菜品带着 `isBuiltin: true` 批量插入 `dishes`
  3. 中途失败可删锁重试（"更多"页提供重新导入入口，按菜名跳过已存在）

### 4.6 转盘

- Canvas 2D 绘制扇区（等分、菜名沿半径绘制）、指针固定向上
- 动画：`requestAnimationFrame` 缓动旋转，总角度 = 1800° + 目标扇区角度 ± 抖动
- 结果层用 `t-dialog`，确认 → 落账 → 跳详情页可选

### 4.7 图表

- `echarts-for-weixin` 官方适配，按需构建只含 `bar` / `pie`
- 独立分包 `packages/chart`，避免拖累主包体积
- 容器固定高度 + `enableScroll: false`，避免小程序图表滚动冲突

### 4.8 统计聚合示例

```js
// 当月每日次数（柱状）
records.aggregate()
  .match({ date: db.RegExp({ regexp: '^2025-07' }) })
  .group({ _id: '$date', count: _.sum(1) })
  .end()

// 当月菜品分布（饼图 Top N）
records.aggregate()
  .match({ date: /^2025-07/ })
  .group({ _id: '$dishName', count: _.sum(1) })
  .sort({ count: -1 }).limit(10).end()
```

> F25 注意：提前定菜会写入未来日期的 records（今天/明天/后天三窗口），统计仍按真实时间聚合——未来菜到当天才自然计入，无需排除逻辑；但当日统计（statsAggregate 的 date=今天）不会提前算入未做的菜。

### 4.9 身份与家庭隔离（F24）

```
打开小程序 → ensureIdentity()（api/identity.js，幂等单例）
  ├─ openid：Storage 永久缓存（rmdc_openid）；无缓存才调云函数 login
  └─ member：每次实查 members（isAdmin/familyId 可能被管理员随时改，不可缓存）

member === null → 首页弹昵称引导 → registerMember（familyId='', isAdmin=false）
```

- 落账快照：`addCookRecord(dishId, familyId)` 把成员当前家庭写进记录；换家庭不回溯历史
- 查询过滤：`todayRecords(date, familyId)` / `undoLastTodayRecord(date, familyId)` / `statsAggregate({ from, to, familyId })`；`''`（未分配池）是有效过滤条件，不能因 falsy 被忽略
- 菜谱/原料查询不挂家庭（共用），F19 双层缓存不受影响——缓存只作用于 dishes/ingredients，records 维持直查
- 存量旧记录无 familyId 字段：管理页「归池」一次性补 `''`（`migrateLegacyRecords`，幂等），之后可整池划归目标家庭（`assignPoolToFamily`）

---

## 5. 云开发架构

### 5.1 零云函数策略

| 需求 | 常规做法 | 本项目做法 |
|---|---|---|
| CRUD | 云函数 | 小程序端 SDK + 全开权限规则 |
| 联表 | 云函数 lookup | 冗余字段（§4.4） |
| 删文件 | 云函数 | `wx.cloud.deleteFile` |
| 统计 | 云函数聚合 | 小程序端 aggregate |
| 取 openid | 云函数 | 云函数 `login`（F24 唯一例外，3 行官方模板，身份体系必需） |

> 收益：调试直连数据库、CRUD 无部署环节；代价：权限规则全开（家庭场景可接受）。F24 起引入唯一云函数 `login`（仅取 openid），其余链路仍为零云函数直连；「仅管理员可写」用安全规则表达式实现（见 §3.8），接口层已收敛在 `api/db.js` 与分包内 `packages/family/api.js`（仅管理页使用，放主包会触发「主包未使用文件」告警）。

### 5.2 初始化

```js
// app.js onLaunch（已实现）
if (!wx.cloud) {
  console.error('基础库版本过低');
} else {
  wx.cloud.init({ env: config.envId, traceUser: true });
}
```

环境 ID `cloudbase-d2g5pab4nda21e944` 已写入 `config/index.js`。

### 5.3 资源清单

- 数据库集合：`dishes` / `ingredients` / `records` / `app_meta`（控制台手动建 + 设权限）
- 云函数：`getimgurls`（图链换取）/ `login`（取 openid，F24）
- 云存储目录：`dishes/`
- 环境：个人版默认环境，免费额度足够家庭使用

### 5.4 部署与首次配置（F24 家庭多租户）

按顺序执行一次即可启用家庭功能：

1. **部署云函数 login**：开发者工具 → `cloudfunctions/login` 右键「上传并部署：云端安装依赖」
2. **建集合**：云开发控制台 → 数据库 → 新建 `families`、`members` 两个集合
3. **注册自己**：真机打开小程序 → 首页弹昵称引导 → 填昵称加入花名册（此时 isAdmin=false、未分配池）
4. **设管理员（仅一次）**：控制台 → `members` 集合 → 找到自己那条（按昵认）→ 把 `isAdmin` 改为 `true`
5. **（可选，硬限制）配安全规则**：按 §3.8 给 `families` / `members` 配自定义安全规则（需先拿到自己的 openid：控制台 → 云函数 → login 日志，或 `members` 集合自己记录的 `_openid` 字段）；配完用家人手机验证进不了管理页
6. **初始化数据**：更多页 →「家庭与成员管理」（仅 admin 可见）→ 新建家庭 → 调整成员归属 →「归池历史记录」→「划归家庭」

> 第 3 步后所有人即刻可用（数据进未分配池）；第 4 步后管理入口才出现；第 6 步后历史记录才进入家庭统计。

---

## 6. 页面结构与导航

### 6.1 TabBar（4 个，自定义 tabBar 保留改造）

```
┌────────┬────────┬────────┬────────┐
│ 🍳 首页 │ 📖 菜谱 │ 📊 统计 │ ⚙️ 更多 │
│ 按料找菜│ 列表搜索│ 图表    │ 原料库等│
│ 转盘    │ CRUD    │ 原料榜  │ 设置    │
└────────┴────────┴────────┴────────┘
```

### 6.2 页面清单

| 页面 | 包 | 职责 |
|---|---|---|
| `pages/home` | 主包 | 今日已定卡片、原料多选、匹配结果、转盘 |
| `pages/menu` | 主包 | 菜谱列表：分类 Tab、搜索、卡片流 |
| `pages/stats` | 主包 | 图表：粒度/图型切换、日期翻页、原料榜 |
| `pages/more` | 主包 | 原料库入口、重新导入内置数据、关于 |
| `packages/dish/detail` | 分包 | 菜品详情 |
| `packages/dish/edit` | 分包 | 新增 / 编辑菜品（含原料选择器、图片上传） |
| `packages/ingredient/index` | 分包 | 原料库管理 CRUD |
| `packages/chart/*` | 分包 | echarts 组件 + 按需构建产物 |

---

## 7. 非功能需求

- **加载态**：列表骨架屏（TDesign Skeleton）；操作按钮 loading
- **空态**：菜谱空 / 匹配空 / 记录空 均有插画式提示 + 引导按钮
- **错误处理**：DB 读写失败统一 toast + 控制台日志；表单保留现场
- **并发写**：家庭量级冲突概率极低，依赖云数据库文档级原子性，不做乐观锁
- **可访问性**：主要文字 ≥ 28rpx，操作区 ≥ 80rpx 高，适配长辈使用

---

## 8. 里程碑

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| M0 | 模板裁剪 + 云开发接入 + 集合/权限建立 + seed 导入 | 4 tab 框架跑通，内置菜导入成功 |
| M1 | 原料库 CRUD + 模糊查询 | 增删改查、查重、删除解除引用 |
| M2 | 菜谱 CRUD + 详情 + 三字段搜索 + 图片上传 | 1M 限制生效，gif 超限被拒 |
| M3 | 首页：按料找菜（两种匹配）+ 转盘 + 落账 | 落账后 records 正确，今日卡片刷新 |
| M4 | 统计：日/月/年 + 柱/饼 + 原料榜 | 聚合数字与 records 抽查一致 |
| M5 | 打磨：骨架屏、空态、字体、动效统一 | 家人试用反馈修完 |
| M6 | 家庭多租户：身份/花名册/家庭隔离（F24） | 两成员各落一账互不可见；admin 管理页可用；非 admin 进不了管理页（见 §5.4） |

---

## 9. 模板裁剪清单

**删除**

- `mock/` 全部、`api/request.js`（改造替代）
- 页面：`pages/home`（重建）、`pages/message`、`pages/my`，分包 `chat` / `login` / `loginCode` / `dataCenter` / `release` / `setting` / `search` / `info-edit`
- `app.js` 中 WebSocket、未读数、更新检查逻辑
- `components/card`（示例卡片）

**保留并改造**

- `custom-tab-bar/`（改 4 tab）、TDesign 组件库、Less 变量体系、`config/index.js`
- `utils/`（保留通用工具，eventBus 视需要保留用于列表刷新）

**新增**

- `api/db.js`、`data/builtin-dishes.js`、`packages/*` 四个分包、图表构建产物

---

## 10. 决策定稿记录（2026-08-23 问答后全部敲定）

| # | 决策 | 结论 |
|---|---|---|
| 1 | 按料找菜匹配模式 | ✅ 交集匹配 + 匹配度排序，可切完全匹配 |
| 2 | 图片每菜上限 | ✅ 5 张（用户指定） |
| 3 | 做法 | ✅ 分步骤文本 |
| 4 | 次数统计 | ✅ 全部由 records 聚合，不落计数字段 |
| 5 | 转盘 | ✅ 最多 12 格，候选=筛选结果 |
| 6 | 权限 | ✅ 自定义规则全开 |
| 7 | 记录 | ✅ 不支持编辑；附「撤销当天最后一条」|
| 8 | 内置菜图片 | ✅ 首版 emoji 占位 |
| 9 | 数据库 | ✅ 云开发文档库（MySQL 因增量成本否决）|
| 10 | 分类体系 | ✅ 大类 category(meal/drink) + 细分 tags（主食/素菜/蛋类…，饮品含冷饮/热饮）|
| 11 | 原料用量 | ✅ dishes.ingredients=[{id,name,amount}]，库内只存归一名称 |
| 12 | 原料清洗 | ✅ 导入前归并变体、剔水类、修脏数据，控制在 ~130 个 |
| 13 | 调料 | ✅ isSeasoning 标记，找菜不参与匹配，统计榜默认排除 |
| 14 | cookTime/difficulty | ✅ 保留展示，详情页徽章，上传选填 |
| 15 | 原料重命名撞名 | ✅ 合并引用后删旧 |
| 16 | 餐次 | ✅ 首版不区分，records 预留 mealType 不启用 |

> 其中 #7 建议加一个"撤销当天最后一条"的能力（M5 观察后定），防手滑。
