# 「今天吃什么」进度文档

> 每完成一个部分同步更新一次 · 最近更新：2026-08-28

## 状态总览

```
┌────┬──────────────────────────────┬─────────┐
│ 阶段 │ 内容                          │ 状态     │
├────┼──────────────────────────────┼─────────┤
│ M0 │ 模板裁剪 + 云开发接入 + 骨架      │ ✅ 完成  │
│ M1 │ 原料库 CRUD + 模糊查询          │ ✅ 完成  │
│ M2 │ 菜谱 CRUD + 详情 + 搜索 + 图片  │ ✅ 完成  │
│ M3 │ 按料找菜 + 转盘 + 落账          │ ✅ 完成  │
│ M4 │ 统计图表（日/月/年，柱/饼）      │ ✅ 完成  │
│ M5 │ 打磨：骨架屏/空态/体验          │ ✅ 完成  │
│ M6 │ 家庭/成员身份 + 数据隔离       │ ✅ 完成  │
│ M7 │ 提前定菜（今明后三选+分组卡）    │ ✅ 完成  │
└────┴──────────────────────────────┴─────────┘
```

> M0-M5 已验收；M6 家庭多租户（F24）已完成开发，待真机验收 + 首次 admin 配置（见开发文档 §3.8/§4.9）。

## 分工约定

| 角色 | 模型 | 职责 |
|---|---|---|
| 主代理 | runanytime/z-ai/glm-5.3 | 规划设计、架构决策、diff 审查、进度同步 |
| worker1（frontend-worker） | ltai/NCE-deepseek-v4-flash | 前端组件、接口封装、测试 |

## 任务日志（新 → 旧）

### 2026-08-28 · 任务包 F25：提前定菜（今/明/后三选 + 今明已定分组 + 原料查看 + 跳详情）· 主代理 + worker

- **范围（用户拍板）**：落账可三选日期（今天/明天/后天，默认今天）；首页今日卡升级「今明已定」三分组（存在才显示）；每行「原料」弹层查看（非强制采购清单，只看不勾选）；整行点击直达菜品详情页
- **数据层（worker 交付，主代理审查通过）**
  - addCookRecord(dishId, familyId='', date=dateKey()) 三参：validDateKey 校验（格式正则 + 解析回写一致性，拦截 2026-13-99/2026-02-30 类非法值）非法静默回退今天
  - groupByDate(records, dates) 纯函数导出：按有序日期键分组、组内 createdAt 倒序、忽略组外日期
  - upcomingRecords(familyId='')：dateKey(Date.now()+n*86400000) 取今/明/后（Date 自然进位跨月跨年），复用 fetchAll 分页拉全后内存过滤，返回 { today, tomorrow, dayafter, total }；'' 未分配池为有效条件
  - 测试新增 test/upcoming.test.js 7 用例（非法 date 兜底/合法落库/向后兼容/分组纯函数/三分组+组外忽略/空库），91→98 全过
- **UI 层（worker 交付，主代理审查 + 修复一处时序缺陷）**
  - 落账确认弹层与转盘结果弹层改 t-popup 底部弹层，加「今天/明天/后天」三选按钮（pendingDate 默认 today，每次打开重置）；转盘结果层 z-index 12000 盖住转盘（11500）
  - dateKeyOffset(n) 换算落账日期；toast 文案今天「已记录」/明天后天「已定到 X M/D」（mdLabel 去前导零）
  - refreshToday 改 upcomingRecords 组装 todayGroups[{key,label,rows}]，wxml 空组整组不渲染、全空保留空态文案；组标题「今天 M/D」等
  - 每行「原料」catch:tap 弹底部 popup（ingredientNames 快照，空显「暂无原料记录」）；整行点击 navigateTo /packages/dish/detail?id=（dishId 空不跳）；撤销按钮仅今天组（undoLastTodayRecord 语义不变）；**用户反馈补充：明天/后天组每行「删除」按钮**——deleteRecord(recordId, familyId) 行级删除（家庭快照校验防跨家庭误删，4 个测试用例），确认弹层带菜名+目标日期
  - **主代理审查修复**：init() 里 loadIdentity 与 refreshToday 并行，身份就绪后无人补刷今日卡——家庭成员打开时首刷按 '' 查到空组并停留到下次 onShow（F24 既有缺陷，F25 三分组下更易感知）→ loadIdentity().then(member 存在则 refreshToday(true)) 补刷
- **验证（主代理 automator 全链路自测）**：真实落账「西红柿炒鸡蛋」到明天 → tomorrow 组正确显示（含先调试写入的八宝粥）→ 原料弹层 8 项 → 整行跳详情成功 → 返回 total 正确；exceptions 零；node --test 98/98；eslint 新增代码零错误（存量 4 个与 HEAD 同款仅行号平移）；测试写入的 2 条云库记录已清理，无残留
- **后续加固（同日）**：用户报图片 403——COS 临时链接签名 t（过期时刻）已过而本地 exp 未到，死链继续渲染。修复：imgUrl.js getCached 改以签名 t 优先判过期（提前 20 分钟边距），无 t 回退本地 exp；新增 test/imgurl-expire.test.js 4 用例（102→106），用户清缓存实测恢复
- **文档同步**：开发文档 3.4 records.date 语义（三选+非法兜底）、2.4 选菜模块（三选/今明卡/原料/跳详情/撤销仅今天）、4.8 统计口径（未来日期不计入当日统计）；本记录 + 状态总览 M7
- **改动文件**：api/db.js、test/upcoming.test.js（新）、pages/home/index.{js,wxml,less}、开发文档.md、进度文档.md
- **备注**：转盘候选来自菜品库不受影响；统计按真实时间聚合，未来菜到当天自然计入；买菜清单按用户决定不做（原料仅可查看，不做勾选采购）

### 2026-08-28 · 任务包 F24：家庭多租户（身份/花名册/家庭隔离/管理页）· 主代理 + worker

- **设计定稿（用户问答敲定）**：菜谱/原料/图片全家庭共用，仅 records（今日已定、撤销、统计）按家庭隔离；家庭数量不限可别名；新人打开小程序自动入花名册（填昵称），分配家庭仅管理员可操作；未分配前数据进「未分配池」功能照常可用；records 按落账时刻的家庭快照归属（换家庭不回溯历史）；撤销不做限制（家庭内互信）；家庭不提供删除（防误删有数据家庭）
- **身份体系（新增 api/identity.js + 云函数 login）**
  - cloudfunctions/login/：官方 wx-server-sdk 模板 3 行返回 OPENID（DYNAMIC_CURRENT_ENV 同 getimgurls 款）；F24 起唯一云函数例外，需开发者工具右键部署一次
  - ensureIdentity() 幂等单例：openid Storage 永久缓存（rmdc_openid，免重复调云函数）+ member 每次实查 members（isAdmin/familyId 可能被 admin 随时改，不可缓存）；失败清单例 rethrow 允许重试
  - registerMember(nickname)：trim 非空 → add（_openid 云开发自动写入不手动写）→ 清单例重查返回完整文档；isFamilyAdmin 辅助
- **数据层（新增 packages/family/api.js，初版位于 api/family.js，后因主包未使用告警挪入分包 + db.js 四函数加 familyId）**
  - db.js：addCookRecord(dishId, familyId='') / todayRecords(date, familyId='') / undoLastTodayRecord(date, familyId='') / statsAggregate({from,to,familyId=''})；'' = 未分配池是有效过滤条件，不因 falsy 被忽略；默认参数向后兼容
  - family.js 8 函数：families 建改查（重名校验排除自身）、members 查改归属（'' 移回未分配池）、countUnassignedPool、migrateLegacyRecords（存量无 familyId 字段补 ''，幂等）、assignPoolToFamily（整池划归）；全部直查不走集合缓存（量小一致性敏感）
  - **主代理复核修复**：migrate/assign 原实现单次 .get() 静默漏第 21 条起的记录（小程序端单次上限 20）→ 新增 fetchPoolRecords 分页拉全（对齐 db.js fetchAll 的 orderBy _id + 有限循环模式），移前消除 use-before-define
- **UI 层（worker 交付）**
  - home：loadIdentity 与数据加载并行；refreshToday/落账（确认+转盘）/撤销四处传 member.familyId（未就绪走 ''）；未注册弹昵称引导（底部 t-popup + t-input type=nickname 微信快捷填入，可关不强制，本次会话只弹一次）；json 补注册 t-input
  - stats：load 前 await loadIdentity，聚合传 familyId；非首次 onShow 重置 identityReady 重新实查（身份可能已变）
  - more：onShow 实查身份刷新 isAdmin；「家庭与成员管理」cell 仅 admin 可见（wx:if），navigateTo 分包页
  - packages/family 管理页（4 文件，admin 守卫非 admin toast+navigateBack）：我的信息卡（昵称/家庭名/管理员 tag）+ 家庭区（列表点击改名 + 新建，t-dialog content 插槽内嵌 t-input，已核对 dialog.wxml slot 存在）+ 花名册区（点成员弹底部选家庭，含未分配，可调自己并同步本地 member）+ 未分配池区（条数 + 归池 + 划归二次确认文案写清不可撤销）
  - app.json subpackages 注册 packages/family
- **验证**：node --test 91/91 通过；eslint airbnb 规则集首次真实生效（npm i --no-save eslint-config-airbnb-base 后）：新文件错误仅存量同款循环风格（与 db.js fetchAll 一致，跟随项目现状不处理），home/stats/more/family 页面与 identity/family api 零新增错误；存量 seed/upload/db 旧代码的 30+ 既有 lint 错误不属本任务范围未动
- **验收反馈增强（同日，主代理直接实现）**：未分配池只显示条数无法判断划归 → 新增池明细分页（fetchPoolPage 按 date/createdAt 倒序每页 10 条 + 「加载更多」）与单条划归（assignRecordToFamily + 点击池内记录选家庭单独划入，确认弹层按 mode 分流文案，整池/单条共用选择弹层）；单条划归后本地移除该条并异步校准池计数；node --test 91/91、新代码零新增 lint 错误、事件绑定与方法逐一核对通过
- **分包优化（同日）**：上传时代码质量扫描报「主包内不应存在主包未使用的 JS 文件: api/family.js」（该文件仅分包管理页引用）→ 挪入 packages/family/api.js 并更新 import 路径（./api.js），文件头注释与开发文档 5.1、本记录同步更新
- **文档同步**：开发文档加 2.6 家庭模块、3.6 families/3.7 members 集合、3.8 权限（安全规则 JSON：families 写限 admin、members create 放开 update/delete 限 admin，in 常量数组写法已核对官方文档）、4.9 身份与隔离、5.1 login 例外、5.4 部署与首次配置六步、里程碑 M6；进度文档状态总览加 M6（待验收）+ 本记录
- **改动文件**：cloudfunctions/login/（新 2 文件）、api/identity.js（新）、api/family.js（新）、api/db.js、pages/home/index.{js,json,wxml,less}、pages/stats/index.js、pages/more/index.{js,wxml}、packages/family/（新 4 文件）、app.json、开发文档.md、进度文档.md
- **备注**：未动 git（待用户验收后按 #25 分组提交）；menu 页/dish/ingredient 分包/upload/缓存架构零改动；listFamilies/listMembers 单次 get 的 20 条上限在「几个家庭/几人」量级下不会触达（注释已标明前提）；安全规则配置后需真机验证（见开发文档 5.4 第 5 步）

### 2026-08-23 · 任务包 F23：三项体验增强（即时新增标调料 + 首页模糊搜索 + 菜谱页 fab 避让）· worker1

- **需求 A 编辑页即时新增可手动标记调料（packages/dish/edit.\*）**
  - 背景：onQuickAddIngredient 只用 SEASONING_SET.has(name) 自动标记，酱油/味精等 36 项表外调料漏标 false，落库后统计榜不排除；任务书给 SEASONING_SET 有 36 项（实际文件 37 项，鸡精已在表内，验收场景用「鸡精」仍成立）
  - wxml：quick-add 行改 flex 布局——左侧「＋ 新增原料"词"」（整行 bind:tap 新增）+ 右侧独立「设为调料」t-checkbox 勾选区（外层 view catch:tap="onQuickAddSeasoningTap" 阻断冒泡，勾选不触发新增；t-checkbox 内部 mut-bind 先于父级 catch 触发，勾选/新增互不干扰）；勾选态存 data.quickAddIsSeasoning
  - js：data 新增 quickAddIsSeasoning:false；onIngredientSearch 每次搜索词变化重算 `quickAddIsSeasoning = SEASONING_SET.has(normalizeName(kw))`（表命中默认勾上，用户可手动改）；新增 onQuickAddSeasoningChange（`!!e.detail.checked`，t-checkbox change 事件 detail 带 checked 已核对组件源码）与 onQuickAddSeasoningTap（空占位）；onQuickAddIngredient 改用 `this.data.quickAddIsSeasoning ?? SEASONING_SET.has(name)` 拼入 ingredients，收尾一并清 quickAddIsSeasoning
  - 底层链路未动：ensureIngredient(name, isSeasoning) 已把显式标记落库（api/db.js 187-189）
  - **edit.json 新增 t-checkbox 注册**（任务书文件清单未列，但使用 TDesign 组件必须注册，属需求 A 的必要配套，最小改动仅 1 行）
  - less：.quick-add 改 flex 行（text 区 flex:1 暖橙字 + seasoning 独立区），压缩 t-checkbox 默认 32rpx 垂直内边距（--td-checkbox-vertical-padding: 8rpx）贴合行高
- **需求 B 首页模糊搜索菜名/原料名（pages/home/index.\*）**
  - 搜索框 placeholder 改「搜菜名 / 原料」；输入防抖 300ms 后同时重查原料 chips + refreshMatch（新增 hasDishKeyword 数据位，归一化后非空即进入关键词分支）
  - 新增模块级纯函数 matchDishesByKeyword(dishes, kw)：`dish.name 含词 || dish.ingredientNames 任一含词`（快照文档含 ingredients/ingredientNames 冗余字段，用 ingredientNames 与 matchDishesByIngredients 同源）；纯前端内存操作，不新增云查询；不做拼音
  - refreshMatch 关键词分支：词命中菜 → 已选 chips 非空再与 matchDishesByIngredients(selected, {mode}) 取交集（kwIds Set 过滤）；空态文案「没有找到相关菜品」；无词回到原 chips 匹配/分类浏览/引导态逻辑；wxml 引导态条件补 `!hasDishKeyword`
  - 配套：refreshDishesSnapshot 快照就绪后当 hasDishKeyword 也重跑匹配（防关键词先于快照输入）；openWheel 在关键词搜索时直接以 matchList（内存全量）为候选，不再循环拉全量（避免「搜豆腐开转盘转到全库」不一致）
  - 验收场景自证：输「豆腐」无 chips → 词命中列表；选「猪肉」chip 再输「蛋」→ 交集；清空 → 回引导态
- **需求 C 菜谱页 fab 被自定义 tabBar 截断（pages/menu/index.less）**
  - .fab-add bottom 由 `calc(48rpx + env(safe-area-inset-bottom))` 改为 `calc(200rpx + env(safe-area-inset-bottom))`：自定义 tabBar 高度 112rpx（variable.less）+ 安全区，200rpx 为首页转盘按钮同款真机验证值（F13），悬于 tabBar 之上完整可见可点；z-index 保持 10（无弹层冲突）
  - 底部避让核查：menu 页无 scroll-view（页面自身滚动），列表底部 .list-footer `padding: 24rpx 0 180rpx` 已预留——fab 位于右下（610-718rpx 横向），footer 文案居中（约 303-447rpx），无水平重叠，180rpx 足够，无需补
- **自测**：node --check packages/dish/edit.js、pages/home/index.js 通过；node --test **81/81** 通过（回归，未动 test/——matchDishesByKeyword 为页面内模块级纯函数未导出，无新增用例，符合任务书「纯函数若无新增则不加用例」）；edit.json JSON.parse 通过；grep 核对 wxml 事件（onQuickAddSeasoningChange/Tap、onQuickAddIngredient）与 js 一一对应，全项目无 quick-add 旧样式残留
- **改动文件**：packages/dish/edit.js、packages/dish/edit.wxml、packages/dish/edit.less、packages/dish/edit.json（t-checkbox 注册，必要配套）、pages/home/index.js、pages/home/index.wxml、pages/menu/index.less、进度文档.md（本记录）
- **备注**：未动 git（git status 待提交清单如上）；未碰 api/、utils/、test/、其他页面/组件；编辑页候选勾选/搜索防抖、首页分类浏览/转盘/落账/下拉刷新等既有逻辑零改动（仅 refreshMatch 分支扩展与 openWheel 候选来源判断）；工作区基于 2d5722f 干净起点，仅本任务 7 个生产文件 + 本记录

### 2026-08-23 · 任务包 F22：云数据库全接口集成测试套件（真实环境运行版）· worker1

- **背景**：用户要求把所有接口在真实云环境全生命周期测试一遍（不跑 mock）。主代理将用 miniprogram-automator 驱动开发者工具，通过 more 页的临时方法触发测试并收集结果
- **新增 `api/__itest.js`（underscore 前缀=临时开发工具，不进正式链路）**
  - 导出 `runIntegrationTests({ onLog })`：顺序执行测试清单，每步收集 {step, ok, detail}；onLog(type, msg) 实时输出（页面 console）；任一断言失败仅计 fail 继续后续步骤，整体不抛异常，返回 {pass, fail, results}
  - 测试数据统一「测试__」前缀；开始前步骤 0 先清理历史残留：listDishes keyword 查残留菜 → removeDish 逐个删（先菜后原料顺序安全）、listIngredients 查残留原料 → removeIngredient、今日记录只删「尾部连续」的测试记录（undo 语义，不碰用户真实记录）；云存储 user/ 历史测试图无法枚举（小程序端 wx.cloud 无 listFile API），本次上传的图在 j 步骤即时删除
  - 测试清单 17 步：a. ensureIngredient 新建断言 _id/name/isSeasoning(false)/isNew(true)；b. 重复调用幂等（isNew=false、_id 相同）；c. listIngredients({keyword, force:true}) 命中 ≥1；d. saveDish 新增「测试__丝瓜炒蛋」全字段（category meal、tags、cookTime、difficulty、两原料、steps 3 条、images []）断言返回 _id；e. ★getDish 12 字段逐项断言（_id/name/category/tags/cookTime/difficulty/ingredientIds 长度 2/ingredientNames 含两原料/ingredients[0].name/steps 3 条/images 空数组/isBuiltin undefined）——「保存丢字段」核心复检点；f. ★saveDish 编辑 cookTime 20分钟/difficulty 中等/steps 2 条/tags 换 → getDish 逐项断言新值——「编辑不生效」核心复检点；g. listDishes({force:true}) 含测试菜；h. checkDishName true + getDishByName 文档 name 正确；i. searchDishesByIngredients partial（单选丝瓜）与 complete（两原料）均命中；j. 云图上传：writeFileSync 写最小 JPEG 头字节到 USER_DATA_PATH → wx.cloud.uploadFile(cloudPath user/测试__itest_{时间戳}.jpg，避免历史残留重名) → 断言 fileID 以 cloud:// 开头 → saveDish 编辑 images=[fileID] → getDish 断言 images[0] → deleteImages 删除 → saveDish 清空 images（防 removeDish 对已删 fileID 重复 deleteFile）→ unlink 临时文件；k. addCookRecord(dishId)（实际接口签名，内部取快照）断言 _id/dateKey=今日/dishName/ingredientNames 含丝瓜 + todayRecords 含该条（落账前记录今日统计基线 recordsBaseline）；l. undoLastTodayRecord 撤销 → todayRecords 不再含；m. statsAggregate({from:今日,to:今日}) 今日 count === 基线（撤销后计数一致性）；n. renameIngredient(鸡蛋→鸡蛋改名) → getDish 复查 ingredientNames 含新名不含旧名、ingredientIds 指向新 id；o. removeDish → getDish 捕获异常断言查不到；o2. removeIngredient 两原料 → listIngredients force 无残留；p. 终态复查 listDishes/listIngredients/todayRecords 无「测试__」残留
  - 全部接口走 api/db.js 与 api/upload.js 真实调用；未调用 importBuiltinData / 上传内置图（只读 loadBuiltinImageMap 可跳过不测）
- **修改 `pages/more/index.js`（挂临时触发方法）**：静态 import runIntegrationTests（__itest.js 模块顶层无副作用，加载不触发云调用；注释标明验收后删除）；data 新增 itestRunning 防重复点击；`onRunITests`：showLoading → runIntegrationTests({onLog: console.log('[ITEST]', …)}) → 每步结果逐行 console.log('[ITEST]', step, ✅/❌, detail) → 存 getApp().globalData.itestResult（供 automator 读取）→ toast「接口自测 通过 n / 失败 m」；catch 兜底意外中断（itestResult.fail=-1）
- **修改 `pages/more/index.wxml`（开发者工具区块）**：t-cell-group 内新增「🧪 接口自测(真实库)」t-cell（note「真实云环境全接口测试 · 临时开发工具」），样式与现有按钮一致，bind:tap="onRunITests"
- **自测**：node --check api/__itest.js、pages/more/index.js 通过；node --test **91/91** 通过（回归，__itest 不进 node 测试，未增减用例）；grep 核对 onRunITests 在 js/wxml 一一对应、globalData 挂载与 toast 路径与既有 useToastBehavior 一致
- **改动文件**：api/__itest.js（新增）、pages/more/index.js、pages/more/index.wxml、进度文档.md（本记录）
- **备注**：未动 git；未碰 api/db.js、api/upload.js、test/、其他页面/组件；任务书 i 步注「原料就一个」与实际两原料不符，按「complete 也命中」意图用两原料实现（注释已标明）；云存储历史测试图无法枚举属环境限制（小程序端无 listFile）；工作区含 F10-F21 未提交改动，本次基于现状叠加；验收后删除 __itest.js 与 more 页触发代码

### 2026-08-23 · 任务包 F21：固化 saveDish 写入形状集成测试 · worker1

- **背景**：用户怀疑保存丢数据，主代理已用 /tmp/test-savedish.mjs 诊断脚本验证写入链路正确（mock wx.cloud 全链路）。本任务把该脚本固化为正式 node --test 用例，防止以后重构 CRUD/缓存时回归
- **新增 `test/savedish.test.js`（5 用例，node:test + assert/strict）**
  - mock 结构沿用诊断脚本（已跑通）：command Proxy（`_.set→{__cmd:'set',v}`）、serverDate、collection(name)；where() 返回链式 {limit/skip/orderBy: ()=>chain, get: async()=>({data:[]})}（恒空——ensureIngredient 恒走新增分支、getDish 走单查兜底）；add 记录并写入 Map store；doc(id) 的 update 模拟云端语义（`__cmd:'set'` 整体替换为纯值、纯值直接赋值）；wx.Storage 空实现（L2 直接 miss）；wx mock 在 require('../api/db.js') 前经 globalThis.wx 就位；每用例 resetMock（清空 store/调用记录 + queryCache.markDirty 防 L1 单例串扰，F19 铁律）
  - 用例① 新增分支：全字段 payload → add 收到 12 字段（name/category/tags/cookTime/difficulty/ingredientIds/ingredientNames/ingredients/steps/updatedAt/images/createdAt）；steps/images 为纯数组（无命令包装，F1 根因防回归）；ingredientIds/ingredientNames 与内部 ensure 结果按顺序一致；updatedAt/createdAt 为 serverDate 原样
  - 用例② 编辑分支：saveDish(id) → update 数组字段（steps/images/ingredients）与标量字段（name/category/cookTime/difficulty）均带 `__cmd:'set'` 包装（现状断言）；updatedAt 为 serverDate 原样不包 set；带 id 原料跳过 ensure 不新增 ingredients
  - 用例③ 云端应用语义：mock 应用 update 后文档 cookTime/difficulty/steps/images 为新值
  - 用例④ getDish 读回：update 后 getDish 返回 steps 为新数组
  - 用例⑤ ensureIngredient 新原料：ingredients add 收到 {name, isSeasoning, createdAt}，isNew/name/isSeasoning/_id 返回正确
- **自测**：node --check test/savedish.test.js 通过；node --test **91/91** 通过（原 86 + 新增 5）；单跑新文件 5/5 通过
- **改动文件**：test/savedish.test.js（新增）、进度文档.md（本记录）
- **备注**：未动 git；未碰 api/、utils/、其他 test/ 文件、页面/组件；工作区含 F10-F20 未提交改动，本次基于现状叠加

### 2026-08-23 · 任务包 F20：原料选择弹层交互收口（显式完成 + 标题 ✕ 关闭）· worker1

- **背景/症状**：编辑菜打开原料弹层勾选后，弹层一直开着，只有点弹层外空白才消失——缺显式「完成」收口；多选场景没有明确的收尾入口
- **修改 `packages/dish/edit.js`（新增 onIngredientDone 一个方法）**
  - `onIngredientDone`：`setData({ popupVisible: false, searchKw: '' })` 一次收尾——关弹层 + 清搜索词，与 onQuickAddIngredient 的收尾方式完全一致（下次打开弹层不留旧搜索词）；注释标明与「完成」/「✕」双入口等价
- **修改 `packages/dish/edit.wxml`（弹层两处）**
  - 标题行改 `popup-header` 结构：左侧标题「选择原料」+ 右侧自绘 ✕ 关闭（hover-class 浅橙底），点击等同「完成」——交互与转盘弹层标题栏一致（转盘即用自绘 ✕，未注册 t-icon）
  - `popup-list` 之后新增 `popup-footer`：`t-button block theme="primary"` 文案「完成（{{ingredients.length}}）」带已选数，勾几个一眼可见；点后关弹层 + 清搜索词
- **修改 `packages/dish/edit.less`**
  - 新增 `.popup-header`（flex 两端布局，标题栏整体 margin-bottom 20rpx，原 `.popup-title` 的 margin 移入容器）、`.popup-close`（80rpx 点击区、暖橙 #ff8c42、36rpx ✕，margin-right -16rpx 对齐右缘，与 home 页 `.wheel-popup-close` 同款）、`.popup-close-hover`（#fff1e6 浅橙底）
  - 新增 `.popup-footer`（padding-top 24rpx 与列表间距；弹层本身已有 `calc(32rpx + env(safe-area-inset-bottom))` 底部内边距，无需重复 safe-area）
  - 布局验证：弹层为 flex column + max-height 70vh，footer 固定占一行，`popup-list` 保持 `flex:1 + min-height 300rpx + max-height 50vh` 自动收缩滚动，加按钮后不溢出；t-search 防抖、候选勾选/checked 同步逻辑零改动
- **自测**：node --check packages/dish/edit.js 通过；node --test **86/86** 通过（回归，未动 test/）；grep 核对 onIngredientDone 在 wxml 两处（✕ / 完成按钮）与 js 一一对应，popup-header/close/footer 类名三者一致
- **改动文件**：packages/dish/edit.js、packages/dish/edit.wxml、packages/dish/edit.less
- **备注**：未动 git；未碰其他页面/组件/api/test；工作区含 F10-F19 未提交改动，本次基于现状叠加

### 2026-08-23 · 任务包 F19：恢复双层缓存 + TTL 过期 + 刷新强制穿透 · worker1

- **背景/根因（F3 架构的两个失效缺陷）**
  - 缺陷① L2 永不过期:旧 storageCache 只存 {items, syncTs} 不判 TTL,过期由 L1 内存层负责——L1 清空后 L2 数据仍可无限期命中,跨启动/跨天读到旧列表;缺陷② 刷新路径不穿透:onShow/下拉刷新仍走 loadCollection 缓存命中,家人 B 手机写库后,A 手机整页刷新依旧读到本机旧 L2,「多设备永远旧数据」没有修复。F18-b 为一致性彻底拆缓存,但每次读取 1-2s 全量拉库,列表交互回归慢。
  - 新规则一句话:**用户可见的整页刷新时机强制直查(force);页面内连续交互走缓存**——双层缓存(L1 内存 60s → L2 Storage 5min)只服务页面内交互,整页刷新(onShow/下拉刷新/进入页面)显式 force 穿透直达云库并回填两层,多设备一致性由 TTL + force 双保险恢复
- **重建 `utils/storageCache.js`(带 TTL 版)**
  - 值格式改 {ts, data}(旧 {items, syncTs});get(name, ttlMs) 检查 Date.now()-ts > ttlMs 返回 null(默认 5min,缺省 ttlMs 同默认);remove/clearAll/removeByPrefix/cacheKey 保留;读写全 try/catch 静默降级;旧格式无 data 字段自然返回 null 自愈
- **重建 `utils/queryCache.js`(L1 简版)**
  - QueryCache 类(Map + TTL + markDirty 全清,默认 TTL 60s,数组 get 浅拷贝)+ 全局单例;去掉旧版 invalidate(prefix)(现无调用方,getDish 已直查)
- **`api/db.js`(双层缓存统一入口)**
  - loadCollection(name, fetcher, { force=false, ttlMs=60s }):force=false 时 L1(60s)→ L2(storageCache.get(name, 5min))→ L3 逐层回填;force=true 跳过 L1/L2 直接 fetcher() 成功后回填两层
  - 内部 markDirty(collectionNames):L1 全清 + L2 按集合删(缺省 clearAll);写操作挂点——saveDish(['dishes','ingredients'])、removeDish(['dishes'])、ensureIngredient(仅真正写库时 ['ingredients'],用 wrote 标志避免存在即命中时清缓存)、renameIngredient/removeIngredient(['ingredients','dishes'])、updateDishImages(['dishes'])(任务书未列但同属 dishes 写路径,seed 增量补图后列表须失效)、addCookRecord([`records:${date}`])(防御性,records 直查不走缓存);undoLastTodayRecord 不失效(records 直查)
  - fetchAllDishes 升级为导出包装 loadCollection({force})(首页快照共用);listDishes/listIngredients 新增 opts.force 透传;getDish 单条改直查 fetchAll('dishes')(不走集合缓存);todayRecords 改直查 fetchAll('records',{date})(records 维持直查);searchDishesByIngredients 维持直查(首页用内存快照);statsAggregate 本就直查 fetchAll 不经 loadCollection,统计页无需 force
- **页面接线(force 时机)**
  - 首页:refreshDishesSnapshot 改 fetchAllDishes({force:true})(onShow/下拉刷新共用);下拉刷新 chips 加载 loadIngredients(kw, true) 强制穿透;搜索防抖/分类浏览/触底翻页不穿透走缓存
  - 菜谱 tab:onShow(needsRefresh 返回本页)与下拉刷新 → refreshFirstPage(seq, silent, force=true);loadAllSortedDishes 仅在翻页循环第一页透传 force(首拉即回填,后续页走刚回填缓存,避免同一集合重复全量打库);筛选/搜索切换不穿透
  - 原料页:onLoad → refresh(true) 强制穿透;搜索防抖/操作后刷新走缓存(markDirty 已失效)
  - 统计页/详情页:零改动(statsAggregate/getDish 本就走直查,无需 force)
- **测试**
  - 重建 test/storage-cache.test.js(13 用例):set/get 回环+值格式、cacheKey 幂等、浅拷贝防污染、TTL 未过期/过期/缺省 ttlMs、remove、clearAll 只清前缀、removeByPrefix 保 exceptKey、get/set/getStorageInfoSync 抛错降级、JSON 损坏+旧格式自愈
  - 新增 test/db-cache.test.js(4 用例,mock wx.cloud + wx.Storage):force 穿透(L2 有旧值也走 L3 并回填两层,回填后非 force 命中 L1 不再打库)、非 force 命中 L2(不打库并回填 L1)、写操作 markDirty 失效(预热缓存→另一设备改云库→本机 saveDish→L2 被清→listDishes 走 L3 见两方数据)、ensureIngredient 写库后 ingredients 缓存失效
  - test/consistency.test.js 同步:setupMockWx 增加 wx.Storage 替身(每用例全新)+ freshEnv 清空 L1 queryCache 单例(防用例间缓存串扰,这是恢复缓存后必改项);原「直查:写后读重新打库」用例改名/注释为「缓存:写后读缓存已失效,listDishes 走 L3 拿最新」(断言不变,语义由直查改为 markDirty 后失效);首用例注释同步
- **自测**:**node --check** 9 个改动 js 全过;**node --test 86/86 通过**(原 77 − 直查用例更名 + storage-cache 13 + db-cache 4,实际新增);grep 确认 force 接线点:首页 onShow 快照(147 行)/下拉刷新 chips、菜谱 onShow(62 行)/下拉刷新(282 行)、原料页 onLoad(46 行)全部就位;grep 残留检查——queryCache/storageCache 仅出现在 api/db.js、utils/、test/ 三处
- **改动文件**:utils/storageCache.js(重建)、utils/queryCache.js(重建)、api/db.js、pages/home/index.js、pages/menu/index.js、packages/ingredient/index.js、test/storage-cache.test.js(重建)、test/db-cache.test.js(新增)、test/consistency.test.js、进度文档.md(本记录)
- **备注**:未动 git;未碰 app.js/packages/dish/edit.js/pages/home/index.json(工作区既有 F18-a/b 改动)、api/seed.js、api/upload.js、pages/stats、packages/dish/detail、components/、其他测试;工作区含 F10-F18 未提交改动,本次基于现状叠加;listIngredients 签名 (kw, {force}) 向后兼容(kw 单参调用不变)

### 2026-08-23 · 任务包 F18-b：拆三层缓存改直查云库（多设备一致性优先）+ 首页匹配内存快照 · worker1

- **背景/架构决策**：三层缓存（L1 内存 queryCache 30s → L2 wx.Storage storageCache → L3 云库）在多设备场景失效——A 手机写库只清自己缓存，B 手机 L1/L2 永远是旧数据。家庭量级（~130 原料 + 80 菜）全量直查毫秒级，决策：**全部读直查云库，彻底删除缓存层**（一致性优先：每台设备每次读取都拿云库最新数据）
- **api/db.js**（直查改造）
  - loadCollection 改为直查 `const list = await fetcher(); return list.slice();`（保留 name 兼容参数，ttlMs 删除）；删除 markDirty / refreshCollection / CACHE_TTL / purgeStaleRecordCaches 及全部写后缓存同步代码；ensureIngredient/saveDish/updateDishImages 的 sync/skipSync 保留为兼容参数（注释标明直查下无作用），seed.js / edit.js 调用方零改动
  - 删除 preloadCoreData（app.js 冷启动不再预热）；fetchAllDishes 提升为导出（首页快照共用）；文件头注释改为直查架构说明
  - searchDishesByIngredients 匹配核心抽成导出纯函数 `matchDishesByIngredients(allDishes, ingredientNames, {mode})`（交集粗筛 + matchScore 排序 + complete 子集过滤，逻辑原样搬）；searchDishesByIngredients 改为 `fetchAllDishes() → matchDishesByIngredients(...)` 包装
- **删文件**：utils/queryCache.js、utils/storageCache.js、test/storage-cache.test.js（任务书指定）+ test/cache.test.js（queryCache 单测随模块删除，验收要求 test/ 零 queryCache 残留）
- **app.js**：删 preloadCoreData import 与调用（冷启动无预热，页面 onShow 直查）
- **首页内存快照（pages/home/index.js + index.json）**
  - 实例字段 this.dishesSnapshot / this.dishesSnapshotDirty；onShow 时 dirty 则 fetchAllDishes 拉全量存快照置 dirty=false；onHide 置 dirty；下拉刷新（index.json 新增 enablePullDownRefresh）强制重拉快照 + 今日记录 + 原料 chips
  - 点选原料匹配改调 `matchDishesByIngredients(this.dishesSnapshot, selected, {mode})`（内存 0ms）；快照未就绪（首次加载）时 loading 兜底，快照就绪后静默重跑匹配渲染；切 tab / 从编辑页返回都走 onShow → dirty 重拉，保证改菜后匹配最新
- **测试（test/consistency.test.js）**
  - 删三层同步用例（L2 回填断言、L2 命中不再打库等），改写为直查语义：写后读 = mock 云库直接反映写入；新增「直查:写后读重新打库取最新数据」用 reads 计数断言每次读取都打库；mock 清除 wxStorage / queryCache / storageCache 引用
  - matchDishesByIngredients 纯函数新增 4 个单测：partial 交集 + matchScore、partial 降序排序（同分按拼音）、complete 子集、空输入/空数据不抛错
- **自测**：node --check api/db.js、app.js、pages/home/index.js、test/consistency.test.js 通过；node --test **69/69** 通过；`grep -r "queryCache\|storageCache" --include="*.js"` 零残留（进度文档 md 豁免）
- **改动文件**：api/db.js、app.js、pages/home/index.js、pages/home/index.json、test/consistency.test.js；删除 utils/queryCache.js、utils/storageCache.js、test/storage-cache.test.js、test/cache.test.js；追加本记录
- **备注**：未动 git；未碰 packages/dish/edit.js（另一任务在改）、api/seed.js（sync/skipSync 兼容参数保持其调用不动）、components/、其他页面

### 2026-08-23 · 任务包 F18-a：编辑页原料「新增原料」三个症状修复 · worker1

- **背景/症状**：onQuickAddIngredient（packages/dish/edit.js）搜索无结果点「＋ 新增原料」只 concat 了 ingredients + syncCandidateChecked，没关弹层（popupVisible）、没清搜索词、没 toast。用户看到：弹框一直开着（症状1）、点击无反馈（症状2）、实际数据已加入（症状3）
- **修改 `packages/dish/edit.js`（仅 onQuickAddIngredient 一个方法）**
  - `setData({ ingredients, popupVisible: false, searchKw: '' })` 一次收尾：关弹层（字段名 popupVisible 已核实 58 行）＋ 清搜索词（防下次打开弹层残留旧词）
  - 追加 `this.onShowToast('#t-toast', `已添加「${name}」`)` 反馈 toast（沿用页面现有 onShowToast helper，签名 (selector, msg) 已核实 123/280 行先例）
  - 移除 syncCandidateChecked 调用：弹层即关、openIngredientPopup 每次打开会重置 searchKw 并重新 searchIngredients('') 拉候选，无需同步
  - 注释补「关弹层 + 清搜索词 + toast 反馈」
- **自测**：node --check packages/dish/edit.js 通过；node --test **88/88** 通过（回归，未动 test/）
- **改动文件**：packages/dish/edit.js
- **备注**：未动 git；未碰其他文件

### 2026-08-23 · 任务包 F17：转盘结果弹层改「先收转盘再弹结果」（绕开 z-index 层级不可靠）· worker1

- **背景/失败原因**：F16 给结果 t-dialog 加 `z-index 12000` 实测仍被转盘 popup 盖住（t-dialog 内部实现即 t-popup，两者平叠，部分环境 z-index 不生效）；且 closeWheel/onWheelVisibleChange 会连带把 wheelResultVisible 置 false（用户关转盘时结果弹窗消失）。不再纠结层级，改交互：转盘停转 → 高亮停留让用户看清 → 自动收起转盘 → 结果弹窗干净弹出（无遮挡）
- **修改 `pages/home/index.js`**（仅此一文件，五处）
  - `onSpinEnd`：保存结果数据但**不立即弹**（wheelResultVisible 不置 true）；高亮停留 800ms 后 `setData({ wheelVisible: false, wheelResultVisible: true })` 一并收转盘+弹结果；定时器无需存实例——800ms 内用户手动关转盘也无害，timeout 的 setData 幂等
  - `onWheelVisibleChange` / `closeWheel`：删除其中 `wheelResultVisible: false`——结果弹窗生命周期独立于转盘，不随转盘关闭；wheelSpinning 重置保留（注释写明解耦原因）
  - `onWheelAgain`（换一个）：关结果 → 重开转盘（wheelVisible: true）→ 因 popup 内容由 wx:if 包裹、spin-wheel 销毁重建，等 400ms 组件 ready + canvas 初始化后再 `selectComponent('#wheel').spin()`，带 `typeof wheel.spin === 'function'` 兜底；不能直接复用旧实例（已销毁）
  - `onWheelConfirm`（就吃它了）：删冗余 `wheelVisible: false`（转盘在停转时已收，只关 wheelResultVisible）
  - `onWheelResultClose`：维持只关 wheelResultVisible（此时转盘已收，注释同步）
- **自测**：node --check pages/home/index.js 通过；node --test **88/88** 通过（回归，未动 test/）；grep 核对 spinend payload 未变、wxml 绑定（onWheelConfirm/onWheelAgain/onWheelResultClose/closeWheel/onWheelVisibleChange）与 js 一一对应
- **改动文件**：pages/home/index.js
- **备注**：未动 git；未碰 components/spin-wheel、api/、wxml/less、其他页面；wxml 上 z-index 12000 与注释保留（无遮无叠场景下无害，本次任务书范围外不动）；工作区含 F10-F16 未提交改动，本次基于现状叠加

### 2026-08-23 · 任务包 F16：转盘结果弹层被转盘 popup 挡住（z-index 层级）· worker1

- **根因**：t-dialog 内部实现本身就是 t-popup（placement=center），其 zIndex 属性默认 11500，与转盘半屏 popup 默认层级完全相同（转盘 popup 也是 11500），两者平叠时结果弹层被转盘 popup 盖住（用户实测看不见「今晚就吃」弹层）
- **修法（任务书方案 1，一次成功）**：`miniprogram_npm/tdesign-miniprogram/dialog/props.js` 确认 dialog 有 `zIndex: { type: Number, value: 11500 }` 属性且透传给内部 popup（dialog.wxml `zIndex="{{zIndex}}"`）；在 pages/home/index.wxml 的结果 t-dialog 上加 `z-index="{{12000}}"`，显式比转盘 popup 高一层，结果弹层（含遮罩）恒盖在转盘之上；wxml 注释写明根因与取值依据
- **「换一个」流程核对（onWheelAgain）**：关结果弹层（wheelResultVisible=false）→ selectComponent('#wheel').spin() 重转 → onSpinEnd 再弹结果弹层，层级值恒为 12000，不受关闭/重开影响；onWheelConfirm 落账关两弹层、onWheelResultClose 仅关结果层均零改动；其余两个 t-dialog（就做这道？/撤销记录）非在 popup 场景弹出，不加层级（任务书范围外）
- **自测**：node --test **88/88** 通过（回归，未动 test/）；本次未改 js 无需 node --check；grep 确认 popup.wxss 默认 z-index 11500、dialog props 默认 11500
- **改动文件**：pages/home/index.wxml
- **备注**：未动 git；未改 index.js/index.less/其他文件；工作区含 F10-F15 未提交改动，本次基于现状叠加

### 2026-08-23 · 任务包 F15：转盘三项体验修复（竖排菜名 + 停转高亮 + popup 残影）· worker1

- **A 菜名改竖排逐字（components/spin-wheel/index.js draw）**
  - 扇区中角线旋转坐标系内从内向外逐字绘制：`chars = Array.from(truncateName(...))`，起点 `startR = R*0.35`，每字步进 `step = round(fontSize*1.15)`，`fillText(chars[k], startR + k*step, 0)`；textAlign center / textBaseline middle、fontSize size*0.05、truncateName 6 字截断全部保留；注释写明竖排从内向外、字列天然居中扇区中线、不溢出
- **B 停转高亮目标扇区（components/spin-wheel/index.js）**
  - draw 增加第二参数 `highlightIndex = -1`；扇区循环 `i === highlightIndex` 时底色换 '#ffb37a'（比三色循环深一档杏橙）并追加 `lineWidth 3 + strokeStyle '#e06a1f'` 深色描边（先 fill 后 stroke 顺序不变，正常扇区 1.5 白线不变）
  - spin() 动画完成回调改为 `this.draw(this.currentAngle, this.targetIndex)`；注释写明：指针固定正上方 + 高亮扇区 = 双重指示选中结果
  - items observer 重绘 / pendingDraw 补绘 / pageLifetimes.hide 结算里的 draw 调用全部不传 highlightIndex（默认 -1 不高亮）
- **C popup 关闭时 canvas 残影（pages/home/index.wxml）**
  - 根因：t-popup 关闭做下滑动画时，type=2d canvas（同层渲染）在 macOS 工具上不跟随 transform，原地悬浮到容器真正 display:none 才消失（0.5-1s 残影）
  - 修法：popup 的 slot 内容整体包 `wx:if="{{wheelVisible}}"`——关闭时内容（含 spin-wheel）立即销毁，canvas 瞬间消失，popup 空壳照常播关闭动画；打开时组件重建 → ready → initCanvasWithRetry（F14 方案一次成功）→ items observer 重绘盘面；组件销毁重建后内部状态全重置无残留，detached 取消 rAF 已有、关闭时正在转也安全；注释写明根因与意图
- **红线核对**：spin() targetAngle 几何公式（270°−centerAngle）、spinend payload {index, item}、pageLifetimes.hide 结算、detached 取消 rAF 零改动（grep 核对）
- **自测**：node --check components/spin-wheel/index.js 通过；node --test **88/88** 通过（回归，未增减用例）
- **改动文件**：components/spin-wheel/index.js、pages/home/index.wxml
- **备注**：未动 git；未碰 db/seed/upload/detail/more 等其他文件；工作区含 F10-F14 未提交改动，本次基于现状叠加

### 2026-08-23 · 任务包 F14：转盘 canvas 初始化改「node 查询 + 尺寸换算」· worker1

- **修改 `components/spin-wheel/index.js`（initCanvasWithRetry 一个方法）**
  - **F13 为何不治本**：F13 用 `.fields({ node: true, size: true })` 同时拿节点与 boundingClientRect，width≤0 时 200ms 重试。用户实测（macOS 工具 3.17.1）10 次全部超时——popup 内 canvas 的 boundingClientRect 在该场景**恒为 0**（弹层展开动画期间 visibility:hidden），重试治标不治本
  - **改法（双保险，零时序依赖）**：① `.fields({ node: true })` 只取节点——节点在 DOM 树即返回，不受 visibility/布局影响；② 尺寸改由系统信息换算：`cssSize = Math.round(600 / 750 * wx.getSystemInfoSync().windowWidth)`，`node.width/height = cssSize * dpr`（dpr 逻辑保留）——600rpx 为 index.less 的 .spin-canvas 定宽常量，代码注释写明两处需同步；③ 重试仅保底 node 拿不到的情况（极旧基础库），上限从 10 降到 3 次，node 查询正常时一次就中；超限仅 console.error 放弃，页面仍可交互
  - pendingDraw 补绘逻辑保留；spin() 里 `!this.canvas || !this.ctx` 兜底 console.error 保留
- **自测**：node --check components/spin-wheel/index.js 通过；node --test **88/88** 通过（回归，未增减用例）；grep 核对 spin() targetAngle 公式（270°−centerAngle）、spinend payload、pageLifetimes.hide 结算、detached 取消 rAF 零改动
- **改动文件**：components/spin-wheel/index.js
- **备注**：未动 git；未碰 db/seed/upload/detail/more 等其他文件；工作区含 F10-F13 未提交改动，本次基于现状叠加（diff 中 SECTOR_COLORS/LIGHT_COUNT/SPIN_DURATION/initCanvasWithRetry 引入等为 F11/F13 既有改动，非本次引入）

### 2026-08-23 · 任务包 F13：修复真机验收两处回归（转盘 canvas 空白 + 匹配列表转盘按钮被 tabBar 盖住）· worker1

- **修复 1 转盘 canvas 空白（components/spin-wheel/index.js）**
  - 根因：spin-wheel 在 t-popup 内，ready() 执行时弹层展开动画（约 300ms）期间组件处于 visibility:hidden，createSelectorQuery 拿到的 boundingClientRect width=0 → cssSize=0 → draw() 首行防御 return → 盘面永远空白；items 之后不再变化，observers 无重绘机会，形成死锁
  - 修法（组件自治，不动页面时序）：把 ready() 的 exec 回调主体抽成 `initCanvasWithRetry()` 方法（query + node/size + dpr 缩放 + pendingDraw 补绘逻辑整体保留），ready() 改为直接调它
  - `info.node` 存在但 `width <= 0` 时启动重试：`this.initRetryCount = (this.initRetryCount || 0) + 1`，`setTimeout(() => this.initCanvasWithRetry(), 200)`，上限 10 次（约 2s，覆盖 300ms 弹层动画）；超限仅 console.error 放弃（页面仍可交互）；拿到非零 width 后完成初始化并清零重试计数，pendingDraw=true 时照常补绘
  - 注释写明根因（popup visibility:hidden 期间 boundingClientRect 为 0 导致死锁）
- **修复 2 匹配列表 >3 项后转盘按钮点不到 / 列表拖不动（pages/home/index.wxml + less）**
  - 根因：wheel-btn 是流内元素，列表一长被顶到页面底端被自定义 tabBar 盖住；手指从底部起滑落在 tabBar 上触摸被吞，体感「列表拖不动」
  - wxml：wheel-btn 保持页面根级（本就在匹配卡片流外，list-footer 之后、popup 之前），注释同步说明 fixed 语义；wx:if/业务条件（wheelDisabled 等）一律未动
  - less：`.wheel-btn` 改 `position: fixed; right: 32rpx; bottom: calc(200rpx + env(safe-area-inset-bottom))`（避让 tabBar，参照菜单页 ➕ 先例），胶囊形（padding 0 36rpx、height 88rpx、border-radius 44rpx、暖橙底白字、投影），加 `z-index: 50`（低于 popup/dialog 遮罩）；`wheel-btn-disabled` 语义保留（置灰 + 去投影）
  - 新增 `.home-page { padding-bottom: 240rpx }`：保证列表最后一张卡不被悬浮按钮盖住（app.less .page 原有底部 padding 56rpx+safe-area 对首页覆盖增大）
- **自测**：node --check components/spin-wheel/index.js、pages/home/index.js 通过；node --test **88/88** 通过（回归，未增减用例）；git diff 核对 spin() targetAngle 公式（270°−centerAngle）、spinend payload、pageLifetimes.hide 结算、detached 清理逻辑零改动
- **改动文件**：components/spin-wheel/index.js、pages/home/index.wxml、pages/home/index.less
- **备注**：未动 git；未碰 db/seed/upload/detail/more 等其他文件；工作区含 F10-F12 未提交改动，本次基于现状叠加（diff 中的扇区配色/灯珠/SPIN_DURATION/popup 关闭按钮等为 F11 既有改动，非本次引入）

### 2026-08-23 · 任务包 F12：removeDish 删除云图时保护内置公共图 · worker1

- **修改 `api/db.js`（removeDish 内置公共图保护）**
  - 顶部新增单向 import `loadBuiltinImageMap`（api/upload.js）：循环依赖检查——upload.js 只 import data/builtin-dishes.js、不依赖 db.js（seed.js 已同款并存 db.js + upload.js 无环），故选最小改动方案：直接顶部 import，无需动态 import 或内联查询
  - removeDish 清理云文件前先 `await loadBuiltinImageMap()` 拿内置图映射，`Object.values(map)` 构建内置公共图集合，然后 `(dish.images || []).filter(isCloudFileId).filter(id => !builtinValues.includes(id))`——只删用户上传云图，内置公共图（cloud://builtin/…）一律保留
  - try/catch 风格沿用：loadBuiltinImageMap 内部读取失败/文档不存在返回 null，此时 builtinValues 为空、按原逻辑全删云图（保守降级，删菜功能不被云库抖动阻断）
  - 注释写清语义：内置图生命周期跟随 app_meta builtin_images 映射，不随单道菜；删菜时若一并删除会留映射死链、重导该菜封面空白
- **修改 `test/consistency.test.js`（同步断言）**
  - deleteFile mock 增强：记录每次删除的 fileID 到 counters.deletedFileList（供断言）
  - 新增 2 用例：① 注入 app_meta builtin_images 映射，内置菜 images=[内置图, 用户图] 删除后 deleteFile 只收到用户图 fileID（内置公共图保留）✓；② 无 app_meta 文档（loadBuiltinImageMap 返回 null）时降级全删云图 ✓
- **自测**：node --check api/db.js、test/consistency.test.js 通过；node --test **88/88** 通过（原 86 + 新增 2）
- **改动文件**：api/db.js、test/consistency.test.js
- **备注**：未动 git；未碰 detail/more/home/spin-wheel/seed/upload/storageCache 等其他文件；removeDish 每次删菜多一次 app_meta 轻查（家庭量级无压力）

### 2026-08-23 · 任务包 F11：随机转盘体验升级（关闭修复 + 盘面美化 + 动画调优）· worker1

- **修复关闭 bug（pages/home/index.js + wxml + less）**
  - `onWheelVisibleChange` visible===false 时补回写 `wheelVisible: false`——t-popup 为受控组件，不回写就永远开着（用户实测点外部无法回收的根因）；同时重置 wheelSpinning/wheelResultVisible 避免状态残留
  - popup 顶部标题栏新增关闭按钮：自绘 ✕（home/index.json 未注册 t-icon，任务书允许自绘），暖橙主色 + 80rpx 点击区（适配长辈）；新增 `closeWheel()`，与遮罩关闭等价，统一回写 wheelVisible/spinning/result
  - 旋转中点关闭走 spin-wheel 既有 hide 结算逻辑，不冲突
- **盘面美化（components/spin-wheel/index.js draw + less）**
  - 外圈：#ff8c42 粗环（lineWidth 8 保留）+ 环上均布 20 颗白色小灯珠（纯静态，圆心在环中心线 R-6，半径 = cssSize 2%）
  - 扇区：配色升级为更鲜明暖色三循环 `#ffe3cc / #ffd1a3 / #fff1e6`，白色分隔线调细至 1.5
  - 菜名文字：字号 = cssSize 5%（加大），文字色加深 `#5a3312`；6 字码点截断与沿半径方向绘制逻辑保留
  - 中心圆钮（wxml/less 覆盖层，交互不变）：径向渐变 `#ff8c42→#e06a1f` + 白字「转」+ 深色描边圈 + 内外阴影，立体感
  - 指针（less）：外层白色三角 + ::after 内层主色三角，白描边增强醒目度
- **动画调优（components/spin-wheel/index.js）**
  - SPIN_DURATION 4000 → 3500（用户要求 2-4 秒）
  - 两段式缓动更真实：前 20% easeInQuad 起步加速 + 后 80% easeOutCubic 长减速，eased(0)=0、eased(0.2)=0.04、eased(1)=1 连续
  - 【红线】targetAngle 几何公式、spinend payload、pageLifetimes.hide 立即结算、detached 取消 rAF 一律未动（git diff 核对零改动）
- **自测**：node --check components/spin-wheel/index.js、pages/home/index.js 通过；node --test **86/86** 通过（回归，未动 test/）；git diff 确认 targetAngle 计算未变
- **改动文件**：components/spin-wheel/index.js、components/spin-wheel/index.less、pages/home/index.js、pages/home/index.wxml、pages/home/index.less
- **备注**：未动 git；未碰 detail/more/seed/upload/db/storageCache 等其他文件

### 2026-08-23 · 任务包 F10：主包瘦身（删本地 80 图，云映射为唯一图片来源）+ 详情页删除二次确认 · worker1

- **删除本地图片资源（用户已授权）**
  - `static/images/`（80 张约 18MB）与 `data/builtin-images.js` 移出项目树（沙箱拦截 rm，沿用 mv 等价删除，备份在 /tmp/rm-f10-20260823/）；主包瘦身约 18MB，图片来源唯一为云端 app_meta `builtin_images` 映射
- **修改 `api/upload.js`（键基准改菜名清单）**
  - 去掉 `import builtin-images`，新增 `import builtinData` + `const BUILTIN_IMAGE_NAMES = builtinData.dishes.map(d => d.name)`（80 菜名，与内置数据同源）
  - `isBuiltinImageMapComplete` 改按 `BUILTIN_IMAGE_NAMES` 逐一判断非空；`uploadBuiltinImages` 的 names 同步改为菜名清单
  - 上传分支保留但注定失败：函数头注释说明「本地源已移除；云映射不完整时需将原图放回 static/images 并恢复映射后重试」；cloudPath 无本地源可取扩展名，恒为 `builtin/{菜名}.jpg`，filePath 传空串；预检已完整时照常返回 `{ alreadyDone: true, uploaded: 0, failed: [] }`（主路径）
  - getExt 仍被 chooseAndUploadImages 使用（用户上传图），保留
- **修改 `api/seed.js`（云映射唯一来源）**
  - 去掉 `import builtin-images`；`const imageMap = cloudMap || {}`（读取失败时空对象 → 内置菜不挂图走 emoji 占位，注释说明）
  - add 分支 `imageMap[dish.name] ? [imageMap[dish.name]] : []` 与 skip 分支 `if (exists.isBuiltin && imageMap[dish.name])` 天然兼容 undefined（不挂图），无语法残留；同步修正补图分支注释（去掉「本地静态路径兜底」表述）
- **修改 `test/upload.test.js`（同步菜名清单断言）**
  - 去掉 builtin-images import 与 extOf；新增 `BUILTIN_IMAGE_NAMES`（builtin-dishes 菜名）
  - 6 用例键基准全部改为菜名清单；cloudPath 断言改为 `builtin/{菜名}.jpg`；删除 filePath `/static/images/` 前缀断言（filePath 现为空串）
- **修改 `packages/dish/detail.{js,wxml}`（删除二次确认，防误删）**
  - 第一层（现有）：点删除 → t-dialog「删除后不可恢复，历史做菜记录保留」→ 确定；`onDeleteConfirm` 不再执行删除，改为关闭第一层 + 打开第二层
  - 第二层（新增）：第二个 t-dialog，标题「再次确认」，content「确定要删除『{{dish.name}}』吗？」，确认按钮 danger 红色（`deleteReconfirmBtn = { content: '删除', theme: 'danger' }` 传入 confirm-btn，TDesign dialog 默认 primary 会被 ButtonProps 覆盖）
  - 第二层 `onDeleteReconfirmConfirm` 才执行 removeDish（沿用 deleting 防重复提交标志）；任一层取消均安全返回（onDeleteCancel / onDeleteReconfirmCancel）
- **自测**：node --test **86/86** 通过（回归，未增减用例）；node --check 全过（api/upload.js、api/seed.js、packages/dish/detail.js、test/upload.test.js）；`ls static/images` 确认目录不存在；grep 零残留——代码层仅剩 api/upload.js 函数头注释里的说明文字（任务书要求），test/image.test.js 的 `/static/images/` 为 isCloudFileId/orderDishImages 纯函数测试输入字面量（F4 重写时保留，非真实文件引用）；detail.wxml 两个 dialog 绑定与 js handler 一一对应，t-dialog 组件已注册
- **备注**：未动 git；未改其他文件/页面/组件/测试；images-upload/（原始备份，主代理维护）未动

### 2026-08-23 · 任务包 F9：内置图上云存储（方案 A）+ 更多页防误操作校验 · worker1

- **新增 `api/upload.js` 两个导出 + 一个纯函数**（内置图批量上云，主包瘦身：80 张图 18MB 不再打主包）
  - `isBuiltinImageMapComplete(map)`：纯函数，判断云映射是否已覆盖 data/builtin-images.js 全部 80 键且值非空
  - `loadBuiltinImageMap()`：查 app_meta 集合 `_id='builtin_images'` 的 map（菜名→cloud:// fileID）；文档不存在/云库不可达均 try/catch 返回 null（调用方回退本地映射）
  - `uploadBuiltinImages({ onProgress })`：入口预检 loadBuiltinImageMap，已完整（80 键）直接返回 `{ alreadyDone: true, uploaded: 0, failed: [] }` 不重复上传；缺失菜名才上传，cloudPath `builtin/{菜名}.{扩展名}`（复用既有 getExt，包内 /static/images 路径直接作 filePath）；并发分批每批 5 张 Promise.all，单张失败不中断收集菜名；全部完成后写回 app_meta：无记录用 add 带 _id（与 seed_lock 同款），已有部分记录用 doc update `_.set` 合并全量 map，add 并发冲突降级为 update；返回 `{ alreadyDone: false, uploaded: 本次新传张数, failed: [菜名...] }`
- **修改 `api/seed.js` 云映射优先**：importBuiltinData 开头 `const cloudMap = await loadBuiltinImageMap()`（从 api/upload.js import，无循环依赖）→ `imageMap = cloudMap || builtinImages`；add 分支挂图与 skip 分支增量补图统一改用 imageMap（云 fileID 优先、本地路径兜底），`builtinStaticPaths` 改为 `Object.values(imageMap)`——旧失效 HTTP/静态路径的内置菜会被增量补图替换为云 fileID，用户云图/当前映射有效值不动
- **修改 `pages/more/index.js + wxml` 防误操作（用户明确要求）**
  - 两个按钮（初始化 / 上传内置图）note 副标题统一加「 · 非必要不使用」（沿用 t-cell note 小字灰样式）
  - goImportBuiltin 点击先 `listDishes({ pageSize: 1 })` 轻查：total===0 直接原流程；total>0 弹 wx.showModal 温和确认「检测到已有菜谱数据。再次初始化仅补齐缺失菜品，不会覆盖你的修改与图片。是否继续？」，确认才走导入、取消直接返回零副作用（预检/确认异常不阻断，与原行为一致）
  - 新增 goUploadBuiltinImages（uploading 防重复点击标志，与 importing 同款）：先 loadBuiltinImageMap + isBuiltinImageMapComplete 预检，已完整 toast「内置图已上传至云端，无需重复操作」不执行不报错；否则 wx.showLoading 实时进度（onProgress 更新 title，title 不变跳过防闪烁，复用初始化按钮写法），完毕按结果 toast（本次上传张数 + failed 数提示可重试，重试只补缺失）
- **修改 `project.config.json`**：packOptions.ignore 新增 `{"type":"folder","value":"images-upload"}`（防原始备份误打包）
- **新增 `test/upload.test.js`（6 用例）**：isBuiltinImageMapComplete 纯函数（null/空/80 完整/缺一/空值）、loadBuiltinImageMap（无记录 null/有记录返回/云库异常降级 null）、全量上传 80 张并写回 app_meta（cloudPath 前缀/onProgress 逐张/值全为 cloud://）、已完整直接返回不重复上传、部分已上云只补缺失 78 张并合并写回保留既有、单张失败收集菜名不中断 + 重试只补缺失 1 张
- **自测**：node --test **86/86** 通过（原 80 + upload 6）；node --check 全过（api/upload.js、api/seed.js、pages/more/index.js、test/upload.test.js）；project.config.json parse 通过；/tmp 完整 mock wx.cloud 验证 seed 云映射优先 4 场景 7 断言全过——云映射存在新导入菜挂 cloud fileID ✓、旧失效 HTTP 路径增量补图替换为 cloud fileID 且不覆盖用户其他字段 ✓、无云映射回退本地 /static/images ✓、用户云图不被覆盖 ✓；wxml 绑定 goUploadBuiltinImages/goImportBuiltin 与 js 一一对应
- **备注**：未动 git；未删 static/images（上传源，等用户操作完成后再处理）；未改其他文件/页面/组件/测试；uploaded 语义为「本次新上传张数」（重试只补缺失时 toast 更准确）；云端 app_meta 需控制台确认已有 seed_lock 后可正常新增 builtin_images 文档

### 2026-08-23 · 任务包 F8：修复真机验收两处 bug（卡片 _id 投影缺失 + 增量补图条件挡住旧数据）· worker1

- **修复 1 菜单/首页卡片拿不到 `_id`（api/db.js）**
  - `DISH_CARD_FIELDS` 首位补 `'_id'`：listDishes 的 pickFields 投影会把它带上，修复菜单卡片详情跳转（navigateTo `detail?id=undefined`）与首页匹配卡落账（addCookRecord 拿不到 dishId）——两处 buildCards/buildMatchCards 都从投影结果取 `dish._id`，之前投影不含 `_id` 导致恒 undefined
  - test/ 无 DISH_CARD_FIELDS 断言（grep 确认），无需同步
- **修复 2 增量补图条件挡旧数据（api/seed.js skip 分支）**
  - 旧条件 `!(exists.images && exists.images.length)` 对非空数组（旧 Unsplash 路径 / ['']）恒 false，永远跳过补图；改为按图片内容判断：`exists.isBuiltin && builtinImages[dish.name]` 且当前 images 既无用户云图、也无当前映射有效静态路径时，`updateDishImages` 补 `[builtinImages[dish.name]]`（skipSync: true）
  - 云图判断用 `p.indexOf('cloud://') === 0` 而非任务书公式的 `isCloudFileId`：实测 `isCloudFileId('https://images.unsplash.com/...')` 返回 true（它只排除 /static/ 前缀），会把旧 Unsplash HTTP 路径误判为云图 → hasCloud=true → 失效旧路径永远补不上，与任务书语义「失效旧路径替换为新内置图」冲突；`cloud://` 前缀是真实云图 fileID 的唯一形态（本应用图片只有 wx.cloud.uploadFile 产生的 cloud:// 与 seed 写入的 /static/ 两种），严格前缀判断对所有场景都正确（代码内已加注释说明）
  - `Object.values(builtinImages)` 提取到循环外一次（builtinStaticPaths），避免每道菜重复计算
- **修复 2b 新增分支不写空串（api/seed.js saveDish 调用处）**
  - `images: [builtinImages[dish.name] || '']` 改为 `builtinImages[dish.name] ? [builtinImages[dish.name]] : []`——映射缺失时存空数组而非 ['']
- **自测**：node --test **80/80** 通过（回归，未新增测试）；node --check api/db.js、api/seed.js 通过；/tmp mock wx.cloud 验证 seed 补图 7 场景全过——旧 Unsplash HTTP 路径补图 ✓、空串数组补图 ✓、用户云图不动 ✓、当前映射有效静态路径不动 ✓、失效旧静态路径补图 ✓、无图补图 ✓、非内置同名菜不动 ✓，其余 73 道默认补图全符合预期（skippedDishes=80、failed=[]）；/tmp 投影验证 listDishes({field: DISH_CARD_FIELDS}) 结果含 _id 且仍裁剪 steps
- **备注**：未动 git；未改其他文件/页面/组件/测试；git diff 中 db.js purgeStaleRecordCaches 与 seed.js reimport 移除为 F7 起未提交的既有改动，本次仅叠加 F8 两处

### 2026-08-23 · 任务包 F7：清理两个审查确认的设计缺陷（S3 缓存泄漏 + S4 死分支）· worker1

- **S3 records 日期缓存键无限累积（存储泄漏）**
  - `utils/storageCache.js`：新增导出 `removeByPrefix(prefix, exceptKey)`——遍历 `wx.getStorageInfoSync().keys` 删除所有以 prefix 开头但 !== exceptKey 的键（与 clearAll 同款遍历写法，try/catch 静默降级风格一致）
  - `api/db.js`：内部新增 `purgeStaleRecordCaches(keepDate)`，调 `storageCache.removeByPrefix('rmdc_records:', storageCache.cacheKey('records:' + keepDate))`（exceptKey 传完整带前缀键）；在 `addCookRecord`（写库后 markDirty 旁）与 `todayRecords`（loadCollection 之后）两处调用——操作某天 records 时顺手清掉其他天的死键，keepDate 当天保留
  - `test/storage-cache.test.js` 新增 3 用例：exceptKey 保留且其余 records 键删除、非 records 前缀键不动；无匹配静默成功；getStorageInfoSync 抛错静默降级
- **S4 seed.js 死分支 + 误导文案（行为零变化）**
  - `api/seed.js`：删除 `reimport` 参数与整个 `if (!reimport)` 卫士块（「库中已有…请先去更多页清空」）、锁冲突 catch 里的 `skipped:true` 返回分支；JSDoc 同步（去 reimport 说明与 skipped 返回字段）；同步清理因此不再使用的 `listDishes/listIngredients` 导入；最终返回去掉 `skipped: false`
  - `pages/more/index.js`：调用改为 `importBuiltinData({ onProgress: ... })`（注释「幂等增量导入」描述保留；更多页本无「清空」功能，误导文案随之消失）
  - `test/consistency.test.js`：删除 2 个初始化卫士用例（库已有数据抛「库中已有」/ 库空正常导入）及不再使用的 seed import，文件头注释同步
- **自测**：node --test **80/80** 通过（原 79 − 2 卫士用例 + 3 removeByPrefix 用例）；node --check 全过（utils/storageCache.js、api/db.js、api/seed.js、pages/more/index.js、两个测试文件）；grep 确认全项目代码无 reimport 残留（仅进度文档历史日志提及）
- **备注**：未动 git；未改其他文件/页面/组件/测试

### 2026-08-23 · 任务包 F6：首页封面图片分层排序 + 难度选项档位统一 · worker1

- **修复 1 首页匹配卡封面未走图片分层排序（pages/home/index.js）**
  - buildMatchCards 改为与 menu/detail 一致：import `orderDishImages`（utils/image.js）；封面 `const cover = orderDishImages(dish.images)[0] || ''`，hasCover 以排序后结果判断（云端 fileID 在前、内置静态图在后，用户上传图不被内置占位图顶掉）
- **修复 2 编辑页难度选项与内置数据/排序档位统一（packages/dish/edit.js）**
  - `DIFFICULTY_OPTIONS` 由 `['简单', '一般', '复杂']` 改为 `['简单', '中等', '较难']`，与内置数据 difficulty 实际取值（简单 48 / 中等 28 / 较难 4）、utils/menuSort.js DIFFICULTY_ORDER 档位完全统一；同步更新 data.difficulty 注释；未动 menuSort.js（未知值兜底保留），test/menuSort.test.js 中「一般/复杂」用例为未知值兜底输入，不依赖编辑页选项，无需同步
- **自测**：node --test **79/79** 通过（回归，未动 test/）；node --check pages/home/index.js、packages/dish/edit.js 通过
- **备注**：未动 git；未改其他文件

### 2026-08-23 · 任务包 F5：内置图片重建与用户图片独立分层 · worker1

- **变更 1 内置图片挂载与增量补图（api/seed.js + api/db.js）**
  - `api/seed.js`：import `data/builtin-images.js` 映射；导入 add 分支恢复 `images: [builtinImages[dish.name] || '']`（映射 80/80 全命中，`|| ''` 仅兜底）；skip 分支的 `checkDishName` 换成新增的 `getDishByName`（返回文档），对 `isBuiltin=true` 且当前无 images 且映射存在的已存在文档，调 `updateDishImages` 用 `_.set` 补充静态首图（`skipSync: true`，不覆盖用户已上传云图，非内置同名菜不补）
  - `api/db.js` 新增两个导出：`getDishByName(name)`（按菜名查文档，不存在返回 null）；`updateDishImages(id, images, {skipSync})`（仅 `_.set` 更新 images 字段 + markDirty，不触碰其他字段，避免 saveDish 全量覆盖用户改动）
- **变更 2 详情页图片区分展示（packages/dish/detail.js + utils/image.js）**
  - `utils/image.js` 新增纯函数 `orderDishImages(images)`：云端 fileID 在前、内置静态图在后，各自保持原相对顺序、空值剔除——保证 UI 顺序恒为 user-cloud → static fallback（不能反），用户上传图不被内置占位图顶掉
  - `packages/dish/detail.js`：loadDish 中 `images = orderDishImages(dish.images)` 后再喂 swiper，置顶判断在此完成
  - 删除图片不删 `/static/images/` 源文件：edit 页 onRemoveImage 既有行为已满足（静态路径不记入 removedFileIds → deleteImages 不清理），本次未改动
- **变更 3 菜单页卡片封面回退恢复（pages/menu/index.js）**
  - buildCards 改为 `cover = orderDishImages(dish.images)[0] || ''`：第一优先云端第一张图 → 其次内置静态图（seed 已写入 images）→ 最终分类 emoji，与 detail 一致；`isCloudFileId` import 替换为 `orderDishImages`
- **自测**：node --test **79/79** 通过（原 74 + image.test.js 新增 5：云端/静态排序、静态路径不清理源文件、空值容错）；node --check 全过（api/seed.js、api/db.js、utils/image.js、packages/dish/detail.js、pages/menu/index.js、test/image.test.js）；/tmp mock wx.cloud 验证 3 场景全过——增量补图（无图内置菜补静态首图）/不覆盖已有云图/非内置同名菜不补/新导入菜挂图、二次重导幂等全 skip 不重复补、reimport=false 首导同样带图
- **备注**：未动 git；未动 images-upload/（原始备份）与 static/images/、data/builtin-images.js、data/builtin-dishes.js；未改 packages/dish/edit.js 与任何其他页面/组件/测试；映射存在性校验 80/80 且文件名大小写与实际文件一致

### 2026-08-23 · 任务包 F4：撤回静态图片占位链路 + 菜单标签折叠（缩动范围收敛）· worker1

- **变更 1 删除 images 本地静态兜底链路**（用户已批准删除）
  - 删除 `static/images/`（80 张 JPG）与 `data/builtin-images.js`（git 已显示 D，工作区不再包含；文件暂移 /tmp/rm-f4-20260823 备份）；由于沙箱权限系统拦截 rm/rmdir（无交互会话可确认），采用 mv 移出项目树实现等价删除
  - `api/seed.js`：去掉 builtinImages 导入与映射，导入时 dishes `images: []`（内置菜不带图）
  - `api/db.js`：removeDish 注释改为仅描述云 fileID（非云路径一律过滤防非法 fileID 报错），过滤调用保留无实际影响
  - `pages/menu/index.js`：封面回退改为「有云端第一张图则显示 → 无则分类 emoji」；`packages/dish/detail.js`：删除 mergeDishImages 调用，图片直接 `dish.images || []`，无图 emoji 占位
  - `utils/image.js`：删除 mergeDishImages（builtinImages 分支逻辑），仅保留 isCloudFileId，注释同步更新
  - `test/image.test.js`：重写为仅覆盖云 fileID 识别与空值边界（去掉本地路径与 mergeDishImages 断言）
- **变更 2 菜谱页细分标签 chips 收折（仅 UI）**
  - 新增 `chipsCollapsed: true` 默认收起、`displayTagChips`（选中项固定在前）、`tagVisibleCount = max(4, 选中数)`；`applyTagChips` 派生补丁在 loadTagOptions / refreshFirstPage 复用；未改 loadTagOptions 数据生成逻辑
  - WXML：标签卡头部新增标题 + 右侧「▾ 全部标签 / ▴ 折叠」切换按钮（总标签数 ≤ 折叠可见数时不显示）；block 包裹按 index 条件渲染
  - LESS：新增 .tag-card-header / .tag-card-title / .tag-toggle 样式（暖橙系）
- **自测**：node --test **74/74** 通过（原 77，减去 image.test.js 删除的 3 个本地路径断言）；node --check 全过（api/seed.js、api/db.js、utils/image.js、pages/menu/index.js、packages/dish/detail.js、packages/dish/edit.js、test/image.test.js）
- **备注**：未动 git（删除态由用户主导提交）；未碰 components/、stats/、records、search/loadTagOptions 数据逻辑；data/builtin-dishes.js 的 M 状态为本次工作前既有，未改动

### 2026-08-23 · 任务包 F3：方案 1 本地全量缓存 + 增删改一致性 + 初始化幂等强化 · worker1

- **新增 `utils/storageCache.js`（三层缓存 L2 持久层）**
  - 键规范：`rmdc_` 前缀 + 集合名（`rmdc_dishes` / `rmdc_ingredients` / `rmdc_records:2026-08-23`），避免与用户手动写入/未来扩展冲突；值 `{ items, syncTs }`（wx.Storage 无原生 TTL，只存 raw items，过期由 L1 内存层负责，ttlMs 为保留参数仅记 syncTs）
  - API：`get`（返回浅拷贝防污染）/ `set` / `remove` / `clearAll`（内嵌 `rmdc_` 前缀匹配遍历删除，不动用户其他键）；读写全包 try/catch，JSON 损坏 / 10MB 超限等异常静默降级仅 console.error，不阻断业务
  - node 端测试以 global.wx 替身注入（in-memory Map 模拟序列化往返），模块顶层不引用 wx
- **改造 `api/db.js`（三层缓存统一入口）**
  - 新增 `loadCollection(name, fetcher, { ttlMs=30s })`：三段读取 L1 内存（queryCache，key=`["collection",name]`）→ L2 Storage（storageCache）→ L3 云库（fetcher）；L1/L2 命中即回填上一层不重打库；L3 成功后回填 L1+L2
  - `markDirty(collectionNames?)`：L1 queryCache 全清 + L2 按集合名删（缺省 clearAll），即「同时清内存 + storage」；`refreshCollection(name, fetcher)` 写库后以 L3 为源重拉回填 L2（失败仅降级，下次读自动走 L3）
  - 五个内部读取单点改造：listDishes（JS 端过滤/排序/分页/投影，等价原数据库 where+orderBy）、getDish（全量缓存 find，miss 单查 L3 兜底）、listIngredients（JS 正则过滤）、todayRecords、searchDishesByIngredients（JS 交集粗筛+精排）均走 loadCollection，共用同一份 dishes/ingredients 缓存
  - 写操作统一「markDirty 双清 + 重拉回填」：saveDish（兼刷 dishes+ingredients，新增 `opts.skipSync` 供 seed 跳过逐条回填）、removeDish、ensureIngredient（新增 `opts.sync`，独立调用同步、saveDish/seed 内部传 false 防循环反复拉全量）、renameIngredient/removeIngredient（刷 dishes+ingredients）、addCookRecord/undoLastTodayRecord（刷当日 records）
  - 新增导出 `preloadCoreData()`：并行拉 dishes/ingredients/今日 records 回填 L1+L2，onLaunch 异步调用不阻塞首屏
- **改造 `api/seed.js` + `app.js`**
  - 初始化卫士：reimport=false 时先 `listDishes({pageSize:1})` + `listIngredients()`，任一非空抛 `'库中已有菜品/原料，禁止初始化导入。请先去更多页清空，或改用增量补导'`；批量导入 ensure 传 `{sync:false}`、saveDish 传 `{skipSync:true}`（仅 markDirty 清缓存，避免导入期间逐条拉全量拖慢）
  - `app.js`：wx.cloud.init 后加 `preloadCoreData()`（wx.cloud.init 逻辑未动）
- **新增 `test/storage-cache.test.js`（11 用例）**：set/get/remove/clearAll 回环、前缀自动补全、cacheKey 幂等、值格式含 syncTs、同键覆盖、clearAll 只清 rmdc_ 前缀、JSON 损坏降级、setStorageSync 超限抛错不中断、getStorageSync 抛错降级、数组浅拷贝防污染
- **新增 `test/consistency.test.js`（10 用例）**：mock wx.cloud 内存库 + wx.Storage 替身 wiring 被测模块——saveDish 新增→listDishes 立即可见、L2 回填新菜、写后读命中 L2 不再打库（读取计数不变）、removeDish→getDish not found+listDishes 不含、L2 同步为最新、addCookRecord→todayRecords 可见、undo 撤销后立即更新、ensureIngredient 独立调用立即可见、初始化卫士抛「库中已有」、库空首次导入不误伤（80 道全导）
- **自测**：node --test **77/77** 通过（回归 56 + storage-cache 11 + consistency 10）；node --check 通过（utils/storageCache.js、api/db.js、api/seed.js、app.js、两个新测试）
- **初始化卫士触发条件**：`importBuiltinData({ reimport: false })` 且 `listDishes({pageSize:1}).total > 0` 或 `listIngredients()` 非空（任一集合已有数据）→ 抛错，文案含「库中已有菜品/原料，禁止初始化导入。请先去更多页清空，或改用增量补导」；pages/more 导出入口仍为 reimport: true 不触发
- **三层缓存同步关键行**：读——`loadCollection`（api/db.js）：`queryCache.get(memKey)`（L1）→ `storageCache.get(name)`（L2）→ `await fetcher()`（L3）+ `storageCache.set` + `queryCache.set` 回填；写——`markDirty(collectionNames)`：`queryCache.markDirty()` + `storageCache.remove/clearAll` 双清，随后 `refreshCollection` 重拉回填 L2（保证“改了就能看到”）
- **备注**：未动 git；未改 components/、utils/stats.js、scripts/、custom-tab-bar/、behaviors/、pages/*（仅 app.js 加 preloadCoreData 一行）；保留 saveDish 原有修复（add 分支纯值写入）与 normalizeDishSteps 历史脏数据兜底、removeDish 本地图过滤；listDishes 由数据库分页改为全量缓存 JS 分页，返回结构 {list,total,hasMore} 不变

### 2026-08-23 · 任务包 F2：三处 UI 迭代 + 内置菜品本地图片静态打包 · worker1

- **改动 1 首页匹配区按需展示（pages/home）**
  - 新增状态 matchSearched（是否已进入查询态）+ matchCategory（''|meal|drink）：未选原料且未选分类时为「未搜索」引导态——不查询、不展示列表，显示文案「选择原料，或让我们抽一个」+ 三个分类入口 chips（全部/餐食/饮品，horizontal，复用 .chip 样式）
  - 点击分类 chip 立即查询（listDishes 新增 category 条件，触底翻页兼容）；chips 出现选中态，再点已选分类取消回到引导态；选原料即进入匹配模式（searchDishesByIngredients）并清空分类选中态；全部取消原料后回到引导态
  - init 改为引导态不查询，首屏骨架由原料 chips + 今日记录加载完成后统一关闭（避免骨架→引导闪空）；空态区分：分类无菜时探测全库，库完全为空才显示「去初始化」引导
  - 转盘候选逻辑（openWheel / fetchAllDishesForWheel）未动；引导态 wheelDisabled 置 false（候选=全部菜品，空库时 openWheel 内既有 toast 兜底）
- **改动 2 菜谱页（pages/menu）**
  - 细分标签 chips 升级为 2 列网格布局（不再横滑），白底 .card 容器内 grid 排列
  - 新增 `utils/menuSort.js`：排序纯函数（node 可单测）——难度 简单(0)→中等(1)→较难(2)，相同难度按名称 localeCompare 'zh'；大类「全部」时餐食整列在前、饮品在后，组内照难度规则；编辑页既有「一般/复杂/空」档位归末档（不猜测映射）
  - 列表改为当前筛选条件下全量拉取 + JS 排序后本地切片翻页（家庭量级循环翻页即可），保证全局排序与分页一致；loadAllSortedDishes 返回 null 丢弃过期响应
- **改动 3 内置菜品本地图片挂载（静态打包）**
  - 新增 `utils/image.js`：isCloudFileId（/static/ 开头为本地路径，非云 fileID）+ mergeDishImages（云端 fileID 优先，无云端图时补内置本地映射图，位置在后），node 可单测
  - `api/seed.js`：导入时 images 填 data/builtin-images.js 映射的单图路径（80/80 全命中，静态目录 80 张图已就位）；本地路径 <image> 直接渲染，微信支持
  - `packages/dish/detail.js`：getDish 后 images 走 mergeDishImages 合并（用户上传的云端图不被本地占位图顶掉；内置菜未传图时展示实拍风格占位图）；wx.previewImage 照常
  - `pages/menu/index.js` 卡片封面：有云端 images 用第一张 → 无则用内置本地映射图（不为空时）→ 两者皆无才显示分类 emoji
  - **必要回归修复**（seed 把本地路径写进 images 后的连锁问题）：`api/db.js removeDish` 删图前 filter 掉本地路径（否则 wx.cloud.deleteFile 收到非法 fileID 抛错导致删除中断）；`packages/dish/edit.js` onRemoveImage 只把云端 fileID 记入 removedFileIds（本地路径不属于云存储，不清理）
- **自测**：node --test **56/56** 通过（回归 45 + 新增 menuSort 6 + image 5）；node --check 全过（api/seed.js、api/db.js、packages/dish/detail.js、packages/dish/edit.js、pages/menu/index.js、pages/home/index.js、utils/menuSort.js、utils/image.js、两个新测试）；mock wx.cloud 验证 removeDish 仅删云端 fileID（本地图被过滤）✓；映射完整性校验 80 菜全命中且文件存在 ✓
- **待工具验证**（无法在命令行验证 UI）：首页引导态/分类 chips 选中态与切换；菜单页 2 列网格渲染与排序观感；详情页/菜单页本地图渲染与 previewImage——需 wx 开发者工具编译检查
- **备注**：未动 git；未改 utils/stats.js、scripts/、custom-tab-bar；data/builtin-images.js 与 static/images/ 为主代理已备好（本次仅使用）；wxml/less 均未新增 TDesign 组件注册

### 2026-08-23 · 紧急修复包 F1（做法缺失 + fab/滚动 + 查询缓存）· worker1（M5 后工作区未提交）

- **修复 1 内置数据「做法」未展示——根因结论**：断点不在内置数据（node 验证 80 道菜 steps 全为数组、元素非空字符串），而在 `api/db.js saveDish`：data 组装时对所有数组字段用 `_.set(...)`（command 对象），**新增（add）分支把 command 对象当普通值写入**。mock wx.cloud 验证：add 分支 steps 落库为 `{"$set":["步骤一","步骤二"]}` 对象（`Array.isArray` 为 false）→ detail 页 `steps.length === 0` 判断与 `wx:for` 渲染失效 → 做法不展示；update 分支的 `_.set` 是合法用法不受影响
  - **修复**：① saveDish 改为先组装纯值 data，update 分支按字段包 `_.set`（updatedAt 的 serverDate 保持原样）、add 分支直接写纯值；② getDish 兜底历史脏数据：`normalizeDishSteps` 识别 `{$set:[...]}` 形状并提取为字符串数组（比任务书公式更精确——任务书公式 `[raw.steps]` 会把 command 对象包进数组仍无法渲染，需识别 $set 提取）；detail/edit 页无需改动即可展示与回填历史数据
- **修复 2 菜单页 fab 只露一半**：采用「全局 app.less `.page` 加 `min-height:100vh; box-sizing:border-box`」方案；menu.wxml 的 fab 现状已在循环块外的根 view（任务书备选条件满足），无需再调 DOM
- **修复 3 详情页长步骤滚动不到底**：检查 detail.less 结论——页面根无 height/overflow 限制（`.detail-cover` 的 overflow:hidden 仅为图片圆角裁剪，非页面根），无需改 detail.less；两个 css 修复点（min-height 100vh + box-sizing / 尾部 `padding-bottom: calc(56rpx + env(safe-area-inset-bottom))`）收敛到 **app.less `.page` 一处**，7 个页面根均用 `.page` 且无局部覆盖，全局生效
- **修复 4 查询速度 + 缓存（主代理设计 / worker 实现）**
  - 新增 `utils/queryCache.js`：内存级 TTL 缓存类 QueryCache（key=`JSON.stringify(参数盐)`、set/get/invalidate(prefix)/markDirty，默认 TTL 5min，get 对数组返回浅拷贝防调用方误改污染；构造函数支持注入时钟便于测试），导出全局单例 queryCache
  - `api/db.js` 接入：listIngredients / searchDishesByIngredients / todayRecords 走 30s 缓存（key 含排序后的原料名，同组选择命中）；getDish 走 60s 缓存 + steps 归一化；saveDish / removeDish / renameIngredient / removeIngredient / addCookRecord 写库成功后 markDirty() 清全量（undoLastTodayRecord 仅 removed:true 实际删除时 markDirty，无记录不写库不失效）
  - `listDishes` 支持 `options.field` 字段投影：导出 `DISH_CARD_FIELDS`（name/category/tags/cookTime/difficulty/ingredientNames/images/isBuiltin），menu 页卡片流、home 页匹配卡片传该投影（steps/ingredients 不进列表查询载荷），home 页 loadTagOptions 只投影 ['tags']、转盘拉全量只投影 ['name']；count 查询不受投影影响；不传 field 默认全量
  - 前端只改 listDishes 调用参数传投影字段，**交互结构未动**；未新增 npm 依赖
- **自测**：node --test **45/45** 通过（38 回归 + 新增 test/cache.test.js 7 用例：命中/未命中/过期/默认 TTL 5min/markDirty 清全量/invalidate 前缀失效/数组浅拷贝防污染）；node --check 通过（utils/queryCache.js、api/db.js、pages/menu/index.js、pages/home/index.js、test/cache.test.js）；/tmp mock wx.cloud 集成验证 13 项断言全过（add 写纯数组、update 保留 command、getDish 历史脏数据兜底、缓存命中不打库、saveDish 后 markDirty 失效、searchDishesByIngredients 同选择只粗筛一次、投影后 steps/ingredients 不在载荷）
- **待工具验证**（无法在命令行验证 UI）：菜单页 `.page` 实际高度 vs 视口一致、fab 完整可见可点；详情页长步骤可滚动到底、底部操作栏不贴底——需 wx 开发者工具右键检查
- **备注**：历史脏数据若 SDK 对其他数组字段（ingredientNames/ingredients/tags）同样原样写入 `{$set:[...]}`，可能影响详情页原料卡与列表页（本次按任务书仅兜底 steps），建议在真实库抽查一条数据确认；未动 git

### 2026-08-23 · 紧急修复：内置菜谱导入不完整（中断无法续传）· 主代理诊断 / worker 修复

- **背景**：importBuiltinData 串行约 900 次云调用（saveDish 内对每个带 id 的原料仍 ensure 查询），耗时 2-3 分钟，中途网络抖动/切后台/限流即整体中断；中断时 seed_lock 已写入，再次点击返回 skipped 无法续传
- **修改 `api/db.js`**：saveDish 的 ingredients 元素已带 id 时跳过 ensureIngredient 查询，直接使用该 id 与归一化后的 name（注释说明：编辑页勾选 / seed 导入传入 id 代表原料已存在，信任即可，减少一次查询）；无 id 时维持 ensure 逻辑不变
- **重构 `api/seed.js`**：`importBuiltinData({ reimport=true, onProgress })`
  - 原料 ensure 阶段建 name→{_id} Map；菜品阶段调 saveDish 时把 Map 里的 id 传入 ingredients（配合 db.js 改动，菜品阶段零冗余 ensure 查询，云调用从约 900 次降到约 130 原料 + 80 查重 + 80 写入）
  - 单道菜 try/catch：失败收集菜名继续后续导入，不中断整体；每道完成后调用 onProgress(done, total)
  - 返回 { skipped, importedDishes, importedIngredients, skippedDishes, failed: [菜名], total }；锁协议保留（add seed_lock 仍照做），reimport 默认改为 true（幂等：已存在的菜按名单跳）
- **修改 `pages/more/index.js`**：按钮调 `importBuiltinData({ reimport: true, onProgress })`；导入中 wx.showLoading 实时进度（onProgress 更新 title，title 不变跳过避免闪烁），完毕不论成败 wx.hideLoading；结果反馈三档组合：importedDishes>0 →「已导入 N 道菜」；skippedDishes===total 且 imported===0 →「内置菜谱已是最新」；failed 非空 → 追加「有 N 道失败，可再次点击重试」
- **自测**：node --check api/db.js / api/seed.js / pages/more/index.js 通过；node --test 38/38 通过（未动 test/）；/tmp 临时 mock wx.cloud 验证：首导 80 道、菜品阶段 0 冗余 ensure 查询（查询数恰为原料数 130）、二次导入全跳过、单菜失败不中断且 failed 收集、再次点击续传只补失败菜
- **备注**：未动 components/ utils/ test/ 数据文件/其他页面/wxml/json；未动 git

### 2026-08-23 · 全局终审 + 项目交付 · 主代理

- **M5 审查通过**；处理自查遗留：删除 menu 页未使用的 t-tag 注册（json 一行）
- **终审校验全过**：7 页面四件套齐全（主包 4 + 分包 3）、31 个 json 全部可解析、TDesign 组件注册路径全部存在、console.log 仅存在于清洗脚本（必要输出）、转盘几何修复未被回退、node --test 38/38
- worker1 已关闭释放；全部任务按「完成→审查→未提交」推进，工作区待用户验收后提交

### 2026-08-23 · M5 打磨收尾：骨架屏 + README 重写 + 全局一致性自查 ✅ · worker1

- **修改 `pages/home/index.{wxml,js,json,less}`**：首屏加载态 t-loading → t-skeleton 骨架屏
  - 组合骨架：今日已定卡（标题 + 两条记录行）+ 冰箱 chips 区（标题 + 一排圆角块）两个 `.card` 内各嵌一个 `t-skeleton`（row-col 配置，animation="gradient"），数据到位后 `loading=false` 隐藏
  - 翻页（matchLoadingMore）/ 静默刷新（refreshMatch/refreshToday 的 silent 模式）不显示骨架，沿用现有 loading/loadingMore 逻辑
  - json 注册 `t-skeleton`，移除不再使用的 `t-loading`（本次改动产生的孤儿）；less 新增 `.skeleton-wrap` / `.skeleton-card` 样式
- **修改 `pages/menu/index.{wxml,js,json,less}`**：首屏加载态 t-loading → t-skeleton 骨架屏
  - 3 行卡片形骨架：`skeletonCards: [1,2,3]` 渲染 3 张 `.card`，每张内嵌一个 `t-skeleton`（左图 160rpx + 右侧两行 row-col 配置）
  - 翻页（loadingMore）/ 下拉刷新 / 静默刷新（refreshFirstPage silent）不显示骨架；json 注册 `t-skeleton`、移除 `t-loading`；less 新增骨架样式
- **重写 `README.md`**（替换 TDesign 模板英文 README，全文中文，约 60 行）：项目名「今天吃什么 · RandomMenu」、功能一览（菜谱管理/原料库/按料找菜/转盘/统计图表）、技术栈（原生小程序 + TDesign + 云开发文档库 + Less）、目录结构树（主包/分包/组件/api/utils/data/test/scripts 各一句话）、快速开始（导入 → 建 4 集合 + 自定义安全规则 `{"read":true,"write":true}` → 更多页初始化内置菜谱）、开发约定（数据层收敛 api/db.js、页面不碰 wx.cloud、纯逻辑进 utils 配 node --test、app.less 暖橙基线）、常用命令（node --test / node scripts/clean-ingredients.js）
- **全局一致性自查**（一次性校验脚本 `/tmp/check-consistency.mjs`，运行后即弃，未入项目）
  - ① navigationBarTitleText 与功能一致 ✅：首页/菜谱/统计/更多/原料库/菜品详情/编辑菜品（新增模式动态改「新增菜品」），全部匹配
  - ② json 注册组件 vs wxml 使用 ⚠️ 发现 1 处未使用：`pages/menu/index.json` 注册了 `t-tag` 但 menu.wxml 未使用（历史遗留，M2-b 注册未删；细分 chips 为自绘 view）。**按任务书只报告不删，待主代理定夺**
  - ③ console.log grep ✅：仅 `scripts/clean-ingredients.js` 14 处（清洗脚本统计报告打印，属必要输出）；页面/组件/api/utils 均无 console.log（仅 console.error）
  - ④ app.json pages/subpackages 与四件套一致性 ✅：7 个页面（4 主包 + ingredient + dish×2）wxml/js/json/less 齐全，反向检查无游离页面
- **自测**：node --test 38/38 通过（回归）；node --check pages/home/index.js、pages/menu/index.js 通过；home/menu json parse 通过；t-skeleton 组件路径存在；home/menu wxml 无遗留 t-loading
- **备注**：本次自改页面移除不再使用的 t-loading 注册（本次改动产生的孤儿，遵循外科手术式修改）；未改 api/ utils/ components/ 其他页面逻辑；未动 git

### 2026-08-23 · M4 统计页正式版 + 自绘图表组件 ✅ · worker1（待主代理审查）

- **新增 `components/bar-chart/index.{js,json,wxml,less}`**：柱状图组件（Canvas 2D 自绘，零外部依赖，样式贴合全局暖橙主题）
  - properties：data（Array，元素 {label, value}，已按展示顺序传入）、unit（String，默认 '次'）
  - canvas type=2d 初始化参照 spin-wheel 的 dpr 缩放模式（ready 取节点 + ctx.scale）；柱体主色 #ff8c42、柱顶圆角（自绘 roundRectPath，不依赖 ctx.roundRect）；value 为 0 的项画 4px 极矮灰柱占位保持列对齐
  - x 轴标签：≤8 项全显，>8 项奇数下标跳过（隔行显示避免重叠）；y 轴不画，仅顶部 3 条虚线网格（25%/50%/75%，浅灰）+ 左上角最大值标注「N 次」
  - 柱子点击：每次绘制记录整列命中矩形（从绘图区顶到底，方便小柱体点击），canvas bind:touchstart 取 e.detail.x/y 命中检测 → triggerEvent('barTap', {label, value})，父页面 toast「xxx：N 次」
  - 空数据：canvas 清空，wxml 覆盖层「暂无数据」居中灰字（canvas 始终渲染保证 ready 可查询；若 wx:if 销毁 canvas 则组件 ready 不会重触发）
  - observers data 变化重绘（未就绪置 pendingDraw，ready 补绘）；宽 100% × 高 420rpx
- **新增 `components/pie-chart/index.{js,json,wxml,less}`**：环形饼图组件（Canvas 2D 自绘）
  - properties：data（Array，元素 {name, value}，已含「其他」聚合项由父层处理）
  - canvas 绘制：暖橙系 6 色循环色板（#ff8c42 #ffb380 #ffd9b8 #e0762f #fff1e6 #7a4a1e），从正上方开始顺时针；扇区透明度 = 0.5 + 0.5×(值/最大值)，值小透明度可降；中心镂空成环形（内半径 0.55R，白色内圆覆盖），环心显示总次数大字 +「共计」小字
  - 图例不用 canvas 画：wxml 渲染两列网格（色点 + 名称 + 次数 + 百分比保留 1 位小数），颜色与画布色板一致（observers 里按 data 顺序分配）
  - 空数据同 bar-chart；observers 变化重建图例并重绘
- **重建 `pages/stats/index.{js,json,wxml,less}`**（替换占位，正式版）
  - 顶部粒度 t-tabs：日 / 月 / 年 / 原料榜；标题旁左右箭头切换上一期/下一期（日 ±1 天、月 ±1 月、年 ±1 年），期文案「8月23日」「2026年8月」「2026年」；未来期禁用（nextDisabled = offset ≥ 0）
  - 图型切换 t-tag「柱状图 / 饼图」（选中态 primary/dark 高亮），切原料榜页签时保持当前选择；切换粒度回当前期（offset=0）
  - 数据源 statsAggregate({from, to})：日=当天 byDish 柱状（每根一道菜）+ byDish 饼图 Top8+其他；月=当月 byDate 柱状（每日 1~31 补零）+ 饼图；年=byDate 按月聚合柱状（1~12 补零）+ 饼图
  - 原料榜页签：byIngredient 排行（名次圆形徽章前三名主色高亮、名称、次数、进度条宽度百分比主色）；顶部 t-switch「含调料」默认关——因 addCookRecord 快照已排除调料，开关仅过滤 records 里历史遗留的调料名（SEASONING_SET 过滤，注释说明）
  - 加载态 t-loading；空态「这一期还没有做菜记录」+ 引导「去首页转一转吧」（柱状全 0 视为空）；onShow 静默刷新（silent 不显示 loading、失败保留旧数据）
  - json 注册 7 个 TDesign 组件 + 两个图表组件；柱点击 toast「xxx：N 次」
- **新增 `utils/stats.js`**：统计页纯逻辑（可在 node 独立测试）
  - computePeriod(granularity, offset, now)：期计算（时间范围 + 文案 + 未来期禁用），now 可注入便于测试
  - buildBarData(granularity, stats, period)：日按菜 / 月按日 1~31 补零 / 年按月聚合 1~12 补零
  - buildPieData(byDish, topN=8)：Top N + 其余聚合「其他」
  - buildIngredientRanking(byIngredient, includeSeasoning, seasoningSet)：过滤调料、名次（前三名 top）、进度条百分比（相对最高次数）
- **新增 `test/stats.test.js`**：13 个用例覆盖期计算（日/月/年、offset、跨月跨年、2 月平/闰、未来期禁用）、柱状组装（补零）、饼图聚合、原料榜过滤与百分比
- **自测**：
  - node --test：38/38 通过（回归 25 + stats 13）
  - node --check：utils/stats.js、pages/stats/index.js、components/bar-chart/index.js、components/pie-chart/index.js、test/stats.test.js 全部通过
  - JSON 合法性：两个组件 json + stats 页 json parse 通过；7 个 TDesign 组件路径存在；wxml 绑定方法（onTabChange/onPrev/onNext/onChartTypeTap/onSeasoningChange/onBarTap/onCanvasTouch）与 js 一一对应
  - 四件套齐全；grep 确认页面/组件无直接 wx.cloud 调用（仅注释）
- **备注**：
  - 任务书「第三页签原料榜」按第 4 个页签实现（t-tabs：日/月/年/原料榜），原料榜时间范围跟随当前粒度与期（granularity 记忆值）
  - 柱状图点击用 bind:touchstart 取 e.detail.x/y（canvas 触摸事件 detail 含相对画布坐标；tap 事件无坐标）
  - eslint 本机 v10 与项目 v8 配置不兼容（M2-b 已注明），按任务书降级 node --check 校验
  - 未动 git；未改 api/db.js / components/spin-wheel / api/upload.js / 其他页面；app.json 未改动（组件在页面 json 注册）

### 2026-08-23 · M3-b 审查修正 + 图表方案决策 · 主代理

- **审查发现并修复转盘几何 bug**：worker 目标角度公式 `A = 360 − centerAngle` 与 canvas 角度系（0° 指向正右方、指针正上方 = 270°）不符，差 270°，导致指针视觉所指与 spinend 事件结果不一致（n=12 时差 3 个扇区）。已改为 `A = 圈数×360 + (270 − centerAngle + 360) % 360 + 偏移`，`node --check` 通过
- **图表方案变更**：开发文档 §4.7 原定 echarts-for-weixin 按需构建；考虑到定制版需网络下载（~500KB）、家庭场景仅需柱/饼两种图表，改为**自绘 canvas 组件**（与转盘同技术栈，零依赖、样式贴合暖橙主题，无需 chart 分包）。待用户确认

### 2026-08-23 · M3-b 随机转盘组件 + 首页接入 ✅ · worker1（主代理审查，含一处修正）

- **新增 `components/spin-wheel/index.{js,json,wxml,less}`**：随机转盘自定义组件四件套（项目首个 components 组件，无外部依赖）
  - properties：items（Array，元素 {id,name}，父组件传入候选）；超过 12 项时 Fisher-Yates 洗牌随机抽样 12 项上盘（盘面可读性优先），绘制前缓存本次盘面名单 spinItems
  - canvas type="2d" 绘制：ready 生命周期 createSelectorQuery().fields({node,size}) 取节点，按 dpr 缩放（ctx.scale）；等分扇区三色循环浅底（浅橙 #fff1e6 / 暖橙浅 #ffd9b8 / 米白 #fdf3e8，与全局主色 #ff8c42 同色系）+ 白色分隔线 + 外圈主色描边；菜名沿半径方向绘制（旋转坐标系 ctx.rotate 到扇区中心角，限 6 字按码点截断加…，字号随盘面尺寸自适应）；中央圆钮「转」与正上方指针用 wxml CSS 覆盖层（三角用 border 绘制）
  - spin()：正在转/items 空/画布未初始化直接 return；目标角度 = 随机基础圈数 5~7 + 目标扇区中心角 + 随机偏移(±扇区角 30%)，换算规则：指针固定正上方，盘面顺时针旋转 angle 后指针指向盘面 -angle 处，故目标角 = 圈数*360 + (360-中心角) + 偏移；requestAnimationFrame + easeOutCubic（1-(1-t)^3）约 4s 插值；结束 triggerEvent('spinend', {index, item})，开始 triggerEvent('spinstart')
  - observers:['items']：变化时刷新 spinItems，未在旋转中则重置角度并重绘（canvas 未就绪时置 pendingDraw，ready 后补绘）；旋转中不重绘防闪动
  - pageLifetimes.hide：若在转，cancelAnimationFrame 后直接结算到目标角度并触发 spinend，避免后台空转；detached 时取消动画帧
  - 绘制失败（极端环境）仅 console.error，不白屏；组件样式自包含（less 不 import app.less，固定色值并注释说明与全局主色一致）；json {component: true}
- **修改 `pages/home/index.{wxml,js,json,less}`**：接入转盘（M3-a 占位按钮替换）
  - 入口按钮：正式「🎰 随机转盘」，候选为空（无匹配/无菜谱）时禁用置灰，文案切换「先选几道菜或添加菜谱」；wheelDisabled 在 refreshMatch 成功后按 matchList 是否为空联动
  - 点击 → t-popup placement="bottom" 半屏弹层（白色背景、顶部圆角），顶部显示候选数（候选 N 道，最多展示 12 道）；内嵌 spin-wheel（id="wheel" items="{{candidates}}"）
  - 候选组装：已选原料时 = 当前匹配结果（matchList 即 searchDishesByIngredients 全量）；未选原料时 = 全部菜品（列表只加载第一页，openWheel 时循环 listDishes 翻页拉全量 fetchAllDishesForWheel）
  - 「开始旋转」按钮调 this.selectComponent('#wheel').spin()；旋转中置灰（spinstart/spinend 事件维护 wheelSpinning）；组件中央「转」也可触发
  - onSpinEnd → t-dialog 结果「今晚就吃：xxx」：confirm「就吃它了」→ 关弹层 → addCookRecord 落账 → toast「已记录」→ 刷新今日卡（复用现有流程）；cancel「换一个」→ 关结果弹层重新 spin；close-on-overlay-click 点遮罩仅关闭结果弹层，保留转盘可再转
  - 弹层遮罩关闭（visible-change=false）重置 wheelSpinning/wheelResultVisible 防状态残留
- **自测**：
  - node --test：25/25 通过（回归，未动 test/）
  - node --check：components/spin-wheel/index.js、pages/home/index.js 全部通过
  - JSON 合法性：组件 json 与 home.json parse 通过；spin-wheel 注册路径 /components/spin-wheel/index 与实际文件四件套对应；t-popup 组件路径存在（miniprogram_npm/tdesign-miniprogram/popup）
  - wxml 绑定核对：新增 10 个方法（onWheelBtnTap/openWheel/fetchAllDishesForWheel/onWheelVisibleChange/onStartSpin/onSpinStart/onSpinEnd/onWheelConfirm/onWheelAgain/onWheelResultClose）全部在 js 中存在；popup visible-change 与 dialog close（closeOnOverlayClick）事件参数与组件源码核对一致
  - 页面无直接 wx.cloud 调用（grep 仅命中文件头注释），转盘候选落账复用 api/db.js addCookRecord
- **备注**：
  - 转盘几何换算说明：盘面坐标系以正上方为 0°，扇区 i 中心角 = i*扇区角 + 扇区角/2；盘面顺时针旋转 angle 后指针（固定正上方）指向盘面 -angle 处，故目标角取 (360-中心角+偏移) 的累加圈数形式，偏移 ±30% 扇区角保证落点仍在目标扇区内
  - 弹层关闭会销毁 spin-wheel 组件（t-popup 用 wx:if 控制渲染），重建后 items 初始值即 candidates，observers 在 ready 前触发置 pendingDraw，ready 后自动补绘，无需页面额外处理
  - 未动 git；未改 api/db.js / api/upload.js / packages/dish / packages/ingredient / pages/menu / test / data / scripts；app.json 未改动（组件按任务书在页面 json 注册）

### 2026-08-23 · M3-a 做菜记录数据层 + 首页按料找菜/手选落账（不含转盘）✅ · worker1

- **修改 `api/db.js`**：追加 records 数据层（未动既有 dishes/ingredients 部分）
  - `dateKey(d = new Date())`：本地时区 YYYY-MM-DD 纯函数（手动拼年月日，不用 toISOString 避免 UTC 错位），导出可 node 单测
  - `addCookRecord(dishId)`：getDish 取快照 → 非调料原料名列表（isSeasoning 字段优先，缺省按 SEASONING_SET 判断；老数据无 ingredients 明细退回 ingredientNames 兜底）→ add `{date, dishId, dishName, ingredientNames, createdAt: serverDate}` → 返回记录文档
  - `todayRecords(date = dateKey())`：fetchAll 后 JS 按 createdAt 倒序
  - `undoLastTodayRecord(date = dateKey())`：where+orderBy desc+limit 1 取当天最后一条 remove；无记录返回 `{removed: false}`
  - `statsAggregate({from, to})`：range 内一次 fetchAll 后 JS 分组（M4 用），返回 `{byDate, byDish, byIngredient}` 均按日期升序 / 次数降序
- **重建 `pages/home/index.{wxml,js,json,less}`**（替换占位）：
  - 今日已定卡：todayRecords() 列表（菜名 + 本地 HH:mm），空态「今天还没定，选一道吧 👇」；每行右侧「撤销」文字按钮 → t-dialog 确认（文案写清「将撤销今天最后一条记录「xx」」）→ undoLastTodayRecord → toast → 刷新
  - 冰箱里有什么：listIngredients('') 全量排除调料 isSeasoning，横向 chips 多选（选中的排前面，已选再点取消；搜索过滤结果外已选原料补到尾部防丢失）；顶部小 t-search（t-class 压缩高度）防抖 300ms
  - 匹配结果区：t-switch 切「有交集即可 / 完全匹配」；已选原料时 searchDishesByIngredients(selectedNames, {mode})，卡片带匹配度百分比徽章（partial 取 matchScore 取整，complete 显示 100%）；点击卡片 → t-dialog「就做这道？」→ addCookRecord → toast「已记录」→ 刷新今日卡
  - 未选原料时：listDishes 第一页全部菜品简版列表（无匹配度徽章），onReachBottom 触底翻页；空态区分「无菜谱→去更多初始化」/「选料无匹配→换个搭配试试」
  - 转盘占位：圆角按钮「🎰 转盘随机（建设中）」置灰不可点（M3-b 接入）
  - onShow 非首次进入静默刷新今日记录（tab 切回数据最新）；requestSeq 序号丢弃过期响应；样式遵循 app.less 基线，less 中文注释
- **新增 `test/date.test.js`**：5 个用例覆盖本地时区构造（`new Date(2026,7,23)` → `'2026-08-23'`）、月/日补零、月末边界（2 月/4 月/大月）、跨年/年初边界、默认参数=本地今天
- **自测**：
  - node --test：25/25 通过（normalize 14 + builtin-data 6 + dateKey 5）
  - node --check：api/db.js、pages/home/index.js、test/date.test.js 全部通过
  - 临时 mock wx.cloud 内存库验证 records 数据层 15 项断言全过（addCookRecord 调料排除/快照、todayRecords 倒序、undo 删最后一条/无记录分支、statsAggregate 三组统计与不含调料）
  - home.json parse 通过；注册 7 个 TDesign 组件路径全部存在（search/switch/loading/empty/button/dialog/toast）；页面无直接 wx.cloud 调用（grep 仅命中文件头注释）
- **备注**：
  - 撤销按钮每行都有，但删除的是当天最后一条记录（undoLastTodayRecord 语义），弹层文案已写清，避免误解
  - mock 验证曾出现 createdAt 同毫秒导致倒序断言失败，实为 mock 时间戳精度问题（真实 serverDate 毫秒级且落账间隔远大于 1ms），修正 mock 时间戳递增后全过
  - 未动 git；未改 api/upload.js / packages/dish / packages/ingredient / pages/menu；api/db.js 仅追加 records 部分

### 2026-08-23 · M2-c 菜品详情页 + 编辑页（替换 M2-b 占位）✅ · worker1

- **新增 `api/upload.js`**：图片能力封装（页面不直接碰 wx.cloud）
  - `chooseAndUploadImages(existingCount, max=5)`：wx.chooseMedia 选图 → 逐张判断：size≤1M 直传；>1M 用 wx.compressImage 循环降质（quality 80→60→40，最后一级 compressedWidth 1280 兜底），仍超 1M 拒绝该张并收集 rejected 文件名；gif 不可压缩、>1M 直接拒绝 → wx.cloud.uploadFile（cloudPath `dishes/{日期时间戳-随机串}.{ext}`，ext 从 tempFilePath 后缀取、默认 jpg）→ 返回 { fileIds, rejected }；单张上传失败先清理本批次已传文件再抛错
  - `deleteImages(fileIds)`：空数组直接返回；wx.cloud.deleteFile 失败仅 console.error 不阻断
- **重建 `packages/dish/detail.{wxml,js,json,less}`**（替换占位）：
  - onLoad(options.id) → getDish 全量渲染，首屏 t-loading，失败 toast + 返回；onShow 从编辑页返回（onHide 置位 needsRefresh）静默刷新
  - 图片区：t-swiper 轮播（fileID 直接渲染）；无图显示大号分类 emoji（meal🍜/drink🥤）浅橙底圆角占位；点击轮播 bind:click → wx.previewImage
  - 头部卡：菜名、t-tag 细分 tags 列表、cookTime · difficulty 徽章、isBuiltin「内置」角标
  - 原料卡：dish.ingredients 逐行「名称 + 用量」，调料行名称后灰字「(调料)」（老数据无 ingredients 时按 ingredientNames + SEASONING_SET 兜底）
  - 做法卡：steps 有序步骤列表（序号圆点 + 文本）；为空显示「暂无做法，快来补充～」
  - 底部操作栏：主按钮「编辑」→ /packages/dish/edit?id=；次按钮「删除」→ t-dialog 确认（「删除后不可恢复，历史做菜记录保留」）→ removeDish → toast 已删除 → navigateBack
- **重建 `packages/dish/edit.{wxml,js,json,less}`**（替换占位，新增/编辑二合一）：
  - 编辑模式 options.id → getDish 回填，标题「编辑菜品」；新增模式标题「新增菜品」（wx.setNavigationBarTitle）
  - 表单：菜名 t-input（必填，空则保存时 toast 拦截）；大类 t-radio（餐食/饮品，切换时清空已选细分 tags）；细分 tags 固定 18 项 chips 多选（选中态预计算 active）+ 自定义 tag 输入（回车/「添加」按钮，归一化去重后加入并显示为 chip）；cookTime t-picker（10/15/20/30/45/60分钟）；difficulty t-picker（简单/一般/复杂）
  - 图片九宫格：已选缩略图（右上 ✕ 删除，fileID 记录待保存后 deleteImages 清理）+ 尾部 ➕ 格；点击 ➕ 调 chooseAndUploadImages；rejected 非空 toast「N 张超过 1M 无法压缩，已跳过」；点击缩略图 wx.previewImage；最多 5 张
  - 原料编辑区：已选行（名称 + 用量 t-input placeholder「如 2个」+ 删除）+「＋ 添加原料」→ t-popup 半屏弹层：t-search 防抖 300ms 调 listIngredients + t-cell 勾选列表（右侧 ✓ 圆点，已选预计算 checked）；搜索无结果首行「＋ 新增原料"关键字"」，点击即加入已选（不必先落库，保存时 saveDish 内部 ensureIngredient 统一处理）
  - 步骤编辑：动态列表，每步 t-textarea + 上移/下移/删除小按钮 + 底部「＋ 添加步骤」（steps 用 {id,text} 对象数组保证 wx:key 唯一）
  - 保存：菜名空拦截 → checkDishName 命中且非本菜（编辑模式菜名未变跳过查重）→ t-dialog「已存在同名菜，仍要保存吗？」→ saveDish；被移除的旧图片 fileID 保存成功后调 deleteImages 清理；成功 toast → navigateBack
  - 样式遵循 app.less 基线（.card、暖橙 #ff8c42、浅灰背景），less 中文注释
- **自测**：
  - node --test：20/20 通过（回归，未动 test/）
  - node --check：api/upload.js、packages/dish/detail.js、packages/dish/edit.js 全部通过
  - detail.json / edit.json / app.json parse 通过；json 注册的 19 个 TDesign 组件路径全部存在（swiper/dialog/input/radio/radio-group/picker/picker-item/popup/search/cell/textarea/tag/button/loading/toast）
  - 页面无直接 wx.cloud 调用（grep 仅命中文件头注释）；api/db.js 与 pages/menu 未改动（仅新增 api/upload.js）；未动 git
- **备注**：
  - WXML 表达式不支持数组方法调用（indexOf），tagChips 选中态与原料候选 checked 均预计算（与 menu 页 chips 的 active 模式一致）
  - 详情页补了 onShow 静默刷新（与 menu 页 needsRefresh 模式一致）：从编辑页保存返回后展示最新数据；静默刷新失败保留旧数据
  - 图片压缩依赖 wx.getFileSystemManager().getFileInfo 读取压缩后大小（wx.compressImage 成功回调不返回 size）；上传失败会先清理本批次已传文件再抛错，避免孤儿文件

### 2026-08-23 · M2-b 菜谱列表页 + 详情/编辑占位页 ✅ · worker1

- **修改 `app.json`**：subpackages 新增 `{ root: "packages/dish", name: "dish", pages: ["detail", "edit"] }`（本任务仅此一处 app.json 改动）
- **新增 `packages/dish/detail.{wxml,js,json,less}`**：菜品详情占位页，js 读 options.id 存 data，标题「菜品详情」（M2-c 填充）
- **新增 `packages/dish/edit.{wxml,js,json,less}`**：菜品新增/编辑占位页，js 读 options.id 存 data（空为新增），标题「编辑菜品」（M2-c 填充）
- **重建 `pages/menu/index.{wxml,js,json,less}`**（替换占位）：
  - 顶部 t-search：输入防抖 300ms → listDishes 三字段模糊（keyword）；点清除立即恢复全量
  - t-tabs 大类：全部 / 餐食 / 饮品（category ''|'meal'|'drink'）
  - 细分标签 chips：横向 scroll-view，选项来自当前大类下全部菜的 tags 前端聚合去重（循环翻页取全量，按出现次数降序，含「不限」），多选切换（选中高亮，listDishes tags _.in）
  - 卡片流：首图（有 images[0] 用 fileID <image>，无图显示分类 emoji 🍜/🥤 浅橙底大字）、菜名、原料名标签（前 4 个 +「+N」）、cookTime · difficulty 小徽章、isBuiltin「内置」角标
  - 分页：onReachBottom 加载下一页（page+1 追加，无更多底部提示「没有更多了」）；onPullDownRefresh 重置第一页（json enablePullDownRefresh）
  - 空态/加载态：无菜谱 → 引导去「更多」页初始化内置菜谱（t-empty + 去初始化按钮 switchTab）；搜索/筛选无结果 → 「换个关键词试试」；首屏 t-loading，翻页 footer 加载中
  - 点卡片 → wx.navigateTo /packages/dish/detail?id=xxx；右下角 fixed 圆形主色 ➕ 浮动按钮 → /packages/dish/edit
  - onShow 非首次进入且 onHide 已置位（从详情/编辑/更多返回）时自动静默刷新第一页（this.needsRefresh 标记）；requestSeq 序号防快速切换条件时过期响应覆盖新列表
- **自测**：
  - node --test：20/20 通过（回归，未动 test/）
  - node --check：pages/menu/index.js、packages/dish/detail.js、packages/dish/edit.js 全部通过
  - app.json 与三个页面 json parse 通过；分包 root ↔ 页面四件套对应；json 注册的 8 个 TDesign 组件路径全部存在（search/tabs/tab-panel/tag/loading/empty/button/toast）
  - 页面层无直接 wx.cloud 调用（grep 仅命中注释，全部走 api/db.js）
- **备注**：
  - 本机 eslint v10.9.0 与项目 v8 .eslintrc.js 配置不兼容（v10 移除 --no-eslintrc、改 flat config），按任务书改用 node --check 校验并注明；尝试 npx/npm exec 临时 eslint8 环境因沙箱拦截 npm install、且 eslint 无法从项目目录解析 airbnb-base 配置包而不可用（与 M1-b 情况一致）
  - 未动 git；未改 api / utils / test / scripts / data / dishes.json / drinks.json；app.json 仅新增 dish 分包

### 2026-08-23 · M2-a 菜谱数据层 + 内置数据导入（不改 UI 页面）✅ · worker1

- **新增 `utils/seasonings.js`**：调料集合唯一来源 `export const SEASONING_SET`（36 项）；任务书所述「页面 38 项 + 脚本 31 项」与现状不符——两处实为完全相同的 36 项，故按并集收敛为 36 项，消除标记分裂
- **修改 `api/db.js`**：
  - `ensureIngredient(name, isSeasoning?)`：调料标记落库 isSeasoning 字段（显式传入优先，缺省按 SEASONING_SET 判断；已有文档标记不一致时补齐）
  - 新增 dishes 数据层：`listDishes({category, tags, keyword, page, pageSize})`（三字段模糊 `_.or` + RegExp + 更新时间倒序分页，返回 list/total/hasMore）、`searchDishesByIngredients(names, {mode})`（partial=交集按匹配度排序 / complete=子集过滤，调料排除由调用方处理）、`getDish(id)`、`saveDish(dish)`（逐个 ensure → 组装 ingredientIds/ingredientNames → add/update，update 用 `_.set` 全量字段 + serverDate）、`removeDish(id)`（先删云存储图片，空数组跳过；records 历史保留）、`checkDishName(name)`
- **新增 `api/seed.js`**：`importBuiltinData({reimport})` 按 app_meta `_id='seed_lock'` 锁协议（errMsg 含 already exist / -502001 判定冲突）；先逐个 ensure 内置原料（带 isSeasoning），再按菜名跳过已存在 → saveDish 入库（isBuiltin: true）；reimport 只补缺失；返回 { skipped(布尔锁标记), importedDishes, importedIngredients, skippedDishes }
- **修改 `packages/ingredient/index.js`**：本地 SEASONING_SET 改 import 共享常量（第 0 步唯一改动，逻辑不变）
- **修改 `scripts/clean-ingredients.js`**：本地 SEASONING_SET 改相对路径 import（转为 ESM，`import.meta.url` 定位目录；node 运行正常）
- **修改 `pages/more/index.{wxml,js,json}`**：原料库入口下新增 t-cell「初始化内置菜谱」→ 调 importBuiltinData({reimport:false})，成功 toast「已导入 N 道菜」/ 已初始化过，失败 toast 重试；注册 t-toast 组件
- **新增 `test/builtin-data.test.js`**：6 个用例（80 道、name 唯一非空、category 枚举、dish.ingredients 结构、顶层 ingredients name 唯一 + isSeasoning 布尔、调料标记与共享集合一致性）
- **自测**：`node --test` 20/20 通过（含 normalize 14 + builtin-data 6）；`node scripts/clean-ingredients.js` 重跑后统计不变（餐食 64 / 饮品 16、原料 130、调料 31），重生成后测试仍 20/20；node 端 import api/db.js、api/seed.js、utils/seasonings.js 均不抛错；eslint 新增/修改文件无新增问题（剩余 no-await-in-loop/no-restricted-syntax 为 M1-a 既有 for-of+await 循环基线模式，任务书未要求 eslint）
- **备注**：
  - scripts/clean-ingredients.js 使用 `import.meta`（ES2020），项目 `.eslintrc.js` 的 ecmaVersion:2018 无法解析该文件——node 运行不受影响，如需 lint 该文件需提升 ecmaVersion（不在本任务范围）
  - seed 内 ingredientMap 原按任务书建 name→id Map，但菜品走 saveDish 内部会复用 ensure，Map 为死代码已移除（最小化）
  - 未动 git；未改 dishes.json / drinks.json / app.json；data/builtin-dishes.js 仅由脚本重生成（时间戳变化，内容与 M1-a 一致）

### 2026-08-23 · M1-b 原料库管理页（分包 UI）✅ · worker1

- **修改 `app.json`**：新增分包 subpackages `{ root: "packages/ingredient", name: "ingredient", pages: ["index"] }`（本任务仅此一处 app.json 改动）
- **新增 `packages/ingredient/index.{wxml,js,json,less}`**：原料库管理页
  - 顶部 t-search，输入防抖 300ms 后调 listIngredients(kw) 模糊过滤
  - 列表（t-cell 自定义行）：原料名 + 被引用菜品次数（进入页面拉一次 ingredientUsage 建 Map）+ 调料行「调料」小标签（SEASONING_SET 与清洗脚本保持一致）
  - 排序：使用次数降序，其次按名称
  - 新增：底部主按钮 → t-dialog + t-input → ensureIngredient；已存在 toast「已存在」并高亮该行 2s
  - 重命名：行点击 → 弹层预填 → renameIngredient；merged 时 toast「已与既有原料合并」；随后刷新列表与次数
  - 删除：行右侧删除按钮 → t-dialog 确认，文案「将解除 N 道菜品的关联，不会删除菜品」（N 来自 usage Map）→ removeIngredient
  - 空态 / 加载态 / 失败统一 toast「操作失败，请重试」；样式遵循 app.less（暖橙 #ff8c42、.card、浅灰背景）
- **修改 `pages/more/index.{wxml,js,json,less}`**：占位文案 → t-cell-group 入口列表，「原料库」wx.navigateTo 分包；标题「更多」保持
- **自测**：
  - eslint 8（airbnb 基线）对 packages/ingredient/index.js、pages/more/index.js：0 error / 0 warning
  - node --test：14/14 通过（回归）
  - app.json 与页面 json 合法；分包 root ↔ 文件四件套对应；页面无直接 wx.cloud 调用（全部走 api/db.js）
- **备注**：沙箱内 `npm install` 被权限系统拦截（无法写入项目 node_modules），改用 `npm exec` 临时环境（eslint@8.57.1 + airbnb 等包，缓存目录）完成 lint，未向项目引入任何依赖、未动 git

### 2026-08-23 · M1-a 数据层封装 + 内置原料清洗 ✅ · worker1

- **新增 `utils/normalize.js`**：纯函数 normalizeName（trim / 全角空格→半角 / 连续空格折叠 / 全角字母数字→半角）、splitIngredient（最后空格拆分「名称 数量」）、escapeRegExp
- **新增 `scripts/clean-ingredients.js`**：清洗 dishes.json（64 道）+ drinks.json（16 款）→ 输出 `data/builtin-dishes.js`；归并 45 项同义变体（姜丝/姜末/姜片/姜蒜→姜，蒜瓣/蒜末→大蒜，葱花→小葱 等，完整映射表见脚本运行输出，供主代理复核）、剔除 18 条水类（清水/温水/凉开水/冰块/热水）、修正 1 项脏数据（「水果丁（芒果/火龙果）适量」→「什锦水果丁」，名称与用量间无空格，按完整串修正）、调料标记 isSeasoning（31 种）
- **新增 `api/db.js`**：云数据库统一封装 ensureIngredient / listIngredients（模糊查询）/ renameIngredient（撞名时 dishes 旧引用改指既有原料后删旧原料）/ removeIngredient（仅解除菜品引用）/ ingredientUsage（基于 dishes.ingredientNames 聚合）；wx 引用一律在函数内部，node 下 import 不抛错（已实测）
- **新增 `test/normalize.test.js`**：14 个用例覆盖空串、全角、无空格、多空格、正则转义等边界
- **生成 `data/builtin-dishes.js`**：80 道菜（64 餐食 + 16 饮品，category 统一 meal/drink，原 category 与冷热饮 type 并入 tags，isBuiltin: true），原料 165 → 130（≤130 ✓），调料 31 种
- **自测**：`node --test` 14/14 通过；`node scripts/clean-ingredients.js` 成功产出并打印统计报告
- **备注**：Node 26.7 下 `node --test test/` 目录参数被当作模块入口（行为变化），改用 `node --test` 默认搜索 / `node --test "test/*.test.js"` 验证，结果一致；未修改 dishes.json / drinks.json / app.json / 页面组件，未引入新 npm 依赖

### 2026-08-23 · M0 模板裁剪 + 云开发接入 ✅ · 主代理

- **删除**：`mock/`、`api/`、`components/`、`utils/`、`behaviors/`（保留 useToast 后又确认保留）、`.github/`、`static/`、根目录 `config.js`、旧页面 ×10（home/message/my/chat/login/loginCode/dataCenter/release/setting/search）
  - 更正：`behaviors/useToast.js` **保留**（页面 toast 通用封装）
- **保留改造**：`custom-tab-bar/`（3 tab → 4 tab）、`config/`（云环境 ID）、`variable.less`、TDesign 组件库（`miniprogram_npm/`）
- **重建**：`app.json`（4 tab + lazyCodeLoading）、`app.js`（wx.cloud.init）、`app.less`（暖橙主色 #ff8c42 + 圆角卡片基类）、4 个 tab 页骨架（home/menu/stats/more）、`project.config.json`（清旧编译模式，项目名 RandomMenu）
- **云环境**：`cloudbase-d2g5pab4nda21e944` 已写入 `config/index.js`
- **验证**：app.json 页面引用 ↔ 文件存在性校验通过；git 提交
- **审查**：主代理自查，删除清单与开发文档 §9 一致

### 2026-08-23 · 开发文档定稿 ✅ · 主代理

- 数据库定稿：云开发文档库（类 MongoDB，零增量成本，否决 MySQL 方案）
- 决策记录：A/B/C 类全按推荐 + 图片上限 5 张 + emoji 占位
- 内置数据：dishes.json（64 道）+ drinks.json（16 款）入库根目录

## 待办 / 下一步

- [ ] M1：worker1 实现 `api/db.js` 数据层 + 原料库管理页（分包 packages/ingredient）
- [ ] 原料清洗归并脚本（姜丝/姜末→姜，剔除水类，~165 → ~120 个）
- [ ] 云开发控制台：建集合 dishes / ingredients / records / app_meta + 自定义安全规则
- [ ] 内置数据 seed 导入（app_meta 锁协议）
